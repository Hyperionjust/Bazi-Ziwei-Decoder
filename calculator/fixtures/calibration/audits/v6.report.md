# v3.13 F0 审计 v6 结果（最终冻结）

> 首次正式网格运行：2026-08-01。v6 恢复总耗方透干门后通过全部发布门，并消除 v5 对李国杰的无证据漂移；作为 v3.13 生产实现的唯一参数来源。

- 配置 SHA-256：`b0f8f3e2586d0bfa19f3600583096d4f409e79ebc55810ef4d139035f7c69966`
- source gold SHA-256：`0b7e8c713caced406b7dc7c28d2515c8c44e6715b8b17bdb61fa6dc5028c56c2`
- v3.12 快照 SHA-256：`60240ef0d17ecdee4fa7194e522f6b5c4bff89a61a740e2798075328385840e2`
- 网格：96 组；满足方向门和因果门 42 组。
- 固定排序首选：`repeat_scale=0.4, echo_scale=1.75, active_discount=0.5, reduced_discount=0`。
- 结果：3/3 目标翻弱、14✅ 零回退、4◐ 零反向、4◐ 零恶化、因果门零失败。
- 非目标绝对分差：4.16，较 v5 的 6.56 减少 2.40；李国杰从额外 `-2.4` 恢复为零漂移。
- 分项：顾 `group_delta=-11.2`；阮 `group_delta=-2.56/root_delta=-6`；周 `group_delta=-10.5`。
- 阮玲玉月日、月时两条生效巳亥冲分别入账，累计折损率封顶 100%。
- 冼冠生 `-2.56`、虞洽卿 `-1.6` 保持原弱侧，没有误推到反侧；袁克文、李国杰均零漂移。
- 相邻 7 格中 4 格可发布。
- 留一重选 2/22 改变；留出的顾、阮各自不通过，表明两例仍是当前小样本的关键边界，属于已知限制而非隐藏稳健性。

## 冻结生产公式

1. 总门：年/月/时至少透出一枚克泄耗干。
2. F1：只检查年/日/时；同一地支重复至少两次且本气为克泄耗。本支全部克泄耗藏干按层级权与类别权汇总。
3. 无本气同类透干时乘 `repeat_scale=0.4`；有本气同类透干时仅乘 `echo_scale=1.75`，两倍率互斥。
4. F2：只处理冲及月柱的非库六冲；只折正值 `得令+长生`。`生效=0.5`、`减力=0`、`被解=0`，多关系累计封顶 100%。
5. 总分：`得令 + 长生 + 得地 + 得势 + 会局 + 耗方群势 + 冲根修正`。
