# v3.13 F0 审计 v4 结果（冻结留档）

> 首次正式网格运行：2026-08-01。v4 已通过方向与因果门，但参数语义/敏感性仍有改进空间；结果保留，最终选择交由 v5。

- 配置 SHA-256：`cf102eb9241ae28aafb1f01425767ec440cf76ec3b6c174068052b11e5581d34`
- source gold SHA-256：`0b7e8c713caced406b7dc7c28d2515c8c44e6715b8b17bdb61fa6dc5028c56c2`
- v3.12 快照 SHA-256：`60240ef0d17ecdee4fa7194e522f6b5c4bff89a61a740e2798075328385840e2`
- 网格：3861 组；满足方向门和因果门 1215 组。
- 固定排序首选：`linear_scale=1.75, synergy_scale=0, active_discount=0.1, reduced_discount=0`。
- 结果：3/3 目标翻弱、14✅ 零回退、4◐ 零反向、因果门零失败；非目标绝对分差 28.70。
- 分项：顾 `group_delta=-11.2`、阮 `group_delta=-11.2/root_delta=-1.2`、周 `group_delta=-10.5`；阮月支两条生效巳亥冲均入账。
- 相邻 6 格中 3 格可发布；但核心 `linear_scale` 的 1.5 邻格漏顾、2.0 邻格使李国杰回退。留一重选 2/22 改变，留出的顾、阮各自不通过。

## 为何另起 v5

v4 的 `linear_scale` 作用于所有重复支，`synergy_scale` 再叠加 echo，语义容易把“总倍率”和“额外倍率”混淆；且 0.25/0.5 的旧粗网格不足以审视顾盘 1.70→1.75 的窄边界。v5 将两个倍率改为互斥语义：无同类别透干的重复支只乘 repeat_scale，有同类别透干的重复支只乘 echo_scale，不再相加。参数范围在首次 v5 运行前冻结，并显式包含失败邻格。
