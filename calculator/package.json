{
  "name": "bazi-ziwei-calculator",
  "version": "0.1.0",
  "description": "八字+紫微算法层 — Yiqi core (vendored) + enrichBazi 补层",
  "private": true,
  "scripts": {
    "build": "tsc",
    "bundle": "esbuild run-chart.ts dump-text.ts render.ts check-analysis.ts self-update.ts --bundle --platform=node --format=cjs --target=node18 --outdir=dist-bundle",
    "chart": "node dist/run-chart.js",
    "dump": "node dist/dump-text.js",
    "render": "node dist/render.js"
  },
  "dependencies": {
    "lunar-typescript": "^1.8.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "~5.6.2"
  }
}
