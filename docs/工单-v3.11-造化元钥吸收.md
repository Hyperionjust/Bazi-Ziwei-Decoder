# 工单 · v3.11 造化元钥吸收 + 全线待办整理

> 立单日：2026-07-29。来源：作者拍板「完全吸收归纳《造化元钥》，最终超级进化整个 skill」。
> 本工单先整理全部在途线头，再给吸收工程的设计与分批计划。执行按批走，每批独立可验收。

---

## 一、全线待办总盘（按优先级收敛）

### 主线 M · 造化元钥吸收工程（本工单第二节，占大头）

### 支线 S1 · 毛盘暴露的算法层四问题（CHANGELOG v3.10.0「已知问题」）
| # | 问题 | 改法 | 状态 |
|---|---|---|---|
| S1-1 | 破型重级不进方向（1976 岁运并临判「顺」，上轮正交化改差的回退） | 天克地冲/伏吟/岁运并临 → 发用 −1；冲提纲/交接仍只算振幅 | ✅ 2026-07-29（1976 丙辰 顺→平；封顶扣一次；正反例各锁回归） |
| S1-2 | 调候∩扶抑对冲无标记（庚金年两轴打架） | `出口.轴冲突` 字段 + 提示词硬约束「合并叙述」 | 待做·**排在 M3 一起做**（工单 2.5 M3 行已列） |
| S1-3 | 杀格身弱高光年全判逆（格局相神被扶抑忌压倒） | `出口` 拆「格局相神/扶抑忌」，相神所制之神降忌权 | 待做·**动全盘，需韦千里命例对照后再动**（见 S2） |
| S1-4 | 振幅太钝（「静」仅 26%） | 命中条数×权重累加分档 | ✅ 2026-07-29（重4/中1/轻0.3 累加；毛盘实测 静 29%→38%、动 42%→33%） |

### 支线 S2 · 韦千里《批命104例》命例化
先做 5~10 例试点（马占山/吴佩孚/阎锡山等断语最完整者）→ `case_type: 典籍判定`，对照我们算法层的用神判定。
**S1-3 的前置**：韦例里大量官杀格名人盘，正好当「格局相神改法」的对照集。
需先把 `.gitignore` 的 `*.case.json` 一刀切改为只排除现实事件类。

### 支线 S3 · 上上轮改动清单未完成项
| 项 | 内容 | 状态 |
|---|---|---|
| 批2 | confidence_tier 拆四维{旺衰,格局,调候,应期} + bazi-prompt 按论断类型取维度；作用关系[i].引爆窗口 | 待做 |
| 批3 | output-mode-B 流月小表进速览；财路映射补「经营/博弈」轴 | 待做 |
| 批4 | 三处内部矛盾：meta.name「命主」必 FAIL / yunsui 年份区间豁免 / 罕象 slice(0,3) 匹配 | ✅ 2026-07-29（豁免路径入 spec.json；大运起止年白名单；罕象改按 `匹配词` 判。三条各锁正反例） |

### 支线 S4 · 发布线
v3.10.0 已积累 6 个提交未发版。发版点建议放在 M1（吸收第一批）落地后，一起出 3.11.0。
GitHub 推送仍卡在本机凭证（`git push --force origin main`，先推 archive-webupload 保底）。

### 已完成存档（不再列入）
v3.9.1 工程卫生 / 顺逆双轴 / 年份下限 1800 / 调候表外置+快照锁 / 双系校勘 7 格 / 三类校准规程。

---

## 二、主线 M · 造化元钥吸收工程设计

### 2.1 目标重定义：吸收 ≠ 校对

校对（已做的）只回答「取干列表抄没抄错」。**吸收**要把书里真正值钱的东西搬进算法与解读——
书的每一格不是「丙癸」两个字，而是一棵**条件树**：

> 正月甲木：得丙癸透，富贵双全（上）；癸藏丙透，名寒木向阳，主大富贵（上）；
> 倘风水不及，亦不失儒林俊秀（中）；如无丙癸，平常人也（下）；
> 一派庚辛，主一生劳苦，克子刑妻（忌）；一派壬癸又无戊己制，名水泛木浮（忌）；
> 一派戊己支会金局，为财多身弱，富屋贫人（忌）……

现在 skill 只知道第一行的两个字。**丢掉的条件分支恰恰是确定性可判的**——丙透没透、癸藏没藏、
是不是一派庚辛，全部是算法层查盘面就能判的事实。这就是进化空间。

### 2.2 产出形态：tiaohou.json v2（每格从数组升级为条例对象）

```json
"甲/寅": {
  "取干": ["丙", "癸"],
  "条例": [
    { "若": "丙癸双透",            "则": "富贵双全",               "档": "上" },
    { "若": "癸藏支且丙透干",       "则": "寒木向阳,主大富贵",       "档": "上", "名": "寒木向阳", "意象": "早春寒木得暖阳,得势而发" },
    { "若": "无丙无癸",            "则": "平常人也",               "档": "下" },
    { "若": "庚辛成一派",          "则": "一生劳苦,克子刑妻",       "档": "忌" },
    { "若": "壬癸成一派且无戊己",   "则": "水泛木浮",               "档": "忌", "名": "水泛木浮", "意象": "水多无堤,根基漂浮" }
  ],
  "病": [],
  "原文": "…全文引…", "出处": "…", "级别": "佐证"
}
```

> `意象` 是概念级素材（比喻的核心，不是句子），供层3 解读自由展开用；`则` 是典籍原判，照录不改写。

`若` 字段用**受控词表**写（透干/藏支/成一派/支会局/无X/有X制…），保证机器可判——这是整个设计的命门：
条件必须写成算法层能对盘面求值的形式，不能是散文。`病` 字段承接「癸水一见，泄庚湿甲伤丁，故以癸为病」类内容。

**受控词表的管辖边界（作者拍板 2026-07-29）——三层分离，只有第一层受控：**

| 层 | 内容 | 约束 |
|---|---|---|
| 层1 · 盘面判定 | 「若」：丙透没透、是不是一派庚辛 | **受控词表**，机器求值，同盘可复现 |
| 层2 · 典籍论断 | 「则」+「名」+「原文」：富贵双全 / 寒木向阳 | **照录原文**，是数据不是输出 |
| 层3 · 用户解读 | 这对眼前这个人意味着什么 | **自由**。硬约束仅两条：①不得引用未命中的条例名（防编造）②档位不得写成宿命断语（护栏） |

层3 的反同质化是**设计要求**：同一条例在不同盘上的呈现，必须结合该盘的格局/十神结构/当前大运语境重写——
「寒木向阳」在一个食伤旺的盘里可以讲创作端的舒展，在一个官杀重的盘里可以讲被赏识的通道，措辞、切入角度、
落到事业还是心态，全部由 LLM 按盘定。**严禁**在提示词里为任何条例配固定白话句式——v3.7.1 的教训
（正缘画像单一句式 → 全盘同款文案）就是这么来的。schema 里给的是 `意象` 字段（概念级素材，如「早春寒木待暖」），
不是句子模板；评审遍（bazi-poster-review）负责查「条例呈现是否结合了本盘语境」这一语义项，脚本层不管措辞。

### 2.3 四个接线点（「超级进化」的实体）

| # | 接线 | 效果 |
|---|---|---|
| J1 | 新模块 `tiaohou-tiaoli.ts`：对盘面求值每条「若」，输出 `enrichment.调候条例` 命中清单 | 确定性、可复现、进 chart.txt |
| J2 | **护体线升级**（接顺逆双轴）：命中「忌/病」分支 → 护体减分。丁/巳盘见癸透，现在护体照加分，书里明说癸是病——这是当前计分与典籍相悖的实锤点 | 顺逆双轴第一次有典籍级依据 |
| J3 | 解读层：chart.txt 注入命中条例（名/则/意象）→ bazi-prompt 两条硬约束（不引用未命中条例名、档位不写成宿命断语）+ 一条**反同质化要求**（条例呈现必须结合本盘格局/十神/大运语境，同条例不同盘不得同款文案；由评审遍查语义，脚本不管措辞） | 解读引真典有名目、防编造，且**个性化**——条例是弹药不是模板 |
| J4 | 体检器：`--mode` 各线增查「条例名不在命中清单 → FAIL」 | 金标样例同步补条例引用 |

### 2.4 工作路径：知砚斋打底 + 影印本校勘（不逐页抄影印本）

影印本 464 书页逐页视觉读不划算（约 200+ 次读取，跨多个会话，转录劳动大于校勘价值）。改为：

1. **知砚斋电子本（78% 覆盖）程序化抽条例初稿** —— 有文本层，条件句式规整（或/若/如/一派/得），可半自动切分；
2. **人工復核每格条例**（这是编辑判断，不可省）；
3. **影印本按格抽查（作者要求加量）**：每行抽 **4~5 格**视觉核对 + 27 个知砚斋缺失格全部用影印本视觉补——
   合计约 70 格有影印本背书（近六成），且每行必含「条例最多的格」与「与知砚斋本有出入的格」；
4. 每格标注来源与校勘级别，快照锁重新祝福。

> **条例怎么写、词表怎么用、哪些坑每批都会踩——见 `docs/条例撰写规范.md`**（M2 立，M3 起沿用）。
> M2 起三行可并行：一行一个子会话按规范起草，主会话合并 + 机器护栏复核（`则` 照录 / 无死条例 / 无恒真 / 词表 / 取干一致 / 关系）。

### 2.5 分批计划（每批一个会话内可完成、可独立验收）

| 批 | 内容 | 验收 |
|---|---|---|
| M1 ✅ | schema 定稿 + **甲木行 12 格**试点 + `tiaohou-tiaoli.ts` 求值器骨架 + J1 接线 | 甲木 12 格条例过新版 test-tiaohou；样例盘 chart.txt 出现命中条例 |
| M2 ✅ | 乙/丙/丁 三行（36 格）+ J2 护体线升级 | 丁/巳 见癸透的盘护体被扣分（典籍病忌生效）；test-shunni 验收点不回归 |
| M3 | 戊/己/庚 三行 + S1-2 轴冲突标记（顺带） | 行内重复度 3 条疑点随戊/己行吸收一并定案 |
| M4 | 辛/壬/癸 三行 + J3 解读接线 + J4 体检器 | 金标样例含条例引用且体检 ALL PASS |
| M5 | 全表收尾：快照重祝福、影印本抽查记录齐、references/calibration.md 结案、发版 v3.11.0 | npm test 全绿 + release.sh 出包 |

### 2.6 边界（先说清楚，防跑偏）

- **吸收 ≠ 全信**：条例照录典籍，档位（上/中/下/忌）是书的判断不是我们的实证。解读层引用时措辞仍守顺逆/可证伪护栏，「主大富贵」这类断语转写为倾向性表述。
- **两系差异**：遇知砚斋本与影印本条例冲突的格，两版并存标「⚖两系分歧」，不擅自择一。
- **评注与语译**（影印本的徐乐吾评注/李铁笔语译层）**本轮不吸收**，只吸收原文条例。评注是二级材料，等原文层稳了再议。

---

## 三、建议执行顺序（供拍板）

```
M1（吸收试点·甲木行） → S1-1 + 批4小修（半个批的量,顺手） → M2 → S2（韦例试点） → M3 → M4 → S1-3（有韦例对照后再动） → M5 发版 → S3 批2/批3
```

理由：S1-1 与批4 是小而确定的修复，插在 M1/M2 之间不打断主线；S1-3 动全盘方向输出，必须等 S2 的韦例对照集就位才敢动；S3 批2/批3 不依赖任何前置，随时可插空。

---

## 四、执行方式：每批一个新会话（作者拍板 2026-07-29）

大规模改动**不在长会话里连续做**。理由：长上下文会触发压缩丢细节、注意力稀释；本工单立单会话后半段已出现数次可归因于此的错误（相似度误报、己/未假分歧）。M1~M5 每批本就按「一个会话内可完成、独立验收」设计。

**交接依赖的唯一事实源就是本工单 + git 历史 + fixtures**，不依赖任何会话记忆：
- 决策记录：三层分离、抽查量、受控词表边界、执行顺序——全在本工单
- 校勘状态：`calculator/tiaohou.json` 校勘块 + `references/calibration.md`
- 安全网：`cd calculator && npm test`（13 项）——新会话理解偏了会被拦住

**新会话开场白（每批复制，改批号即可）：**

> 读 `Bazi-Ziwei-Decoder/docs/工单-v3.11-造化元钥吸收.md`，执行 M1（甲木行试点）。
> 素材在工作区 `素材/`：知砚斋《穷通宝鉴》电子本打底、李铁笔《造化元钥评注》影印本抽查（每行 4~5 格）。
> 铁律：排盘算法不动；受控词表只管盘面判定层，解读层自由（工单 2.2 三层分离）；改完跑 `cd calculator && npm test` 全绿；改任何 .ts 后 `npm run bundle`。
> 验收标准按工单 2.5 对应批次行。完成后 git commit 并在本工单该批打 ✅。

**批间验收（作者侧，两分钟）**：看 git commit 信息 + `npm test` 结果 + 工单进度标记。

### 进度
- [x] **M1 甲木行试点 + 求值器骨架 + J1** ✅ 2026-07-29

  **落地物**：`tiaohou.json` 新增顶层 `条例` 块（甲木 12 格 / **110 条**，`取干` 一字未动故原快照锁不受影响）+ `_词表` 块；
  新模块 `calculator/bazi-enrich/tiaohou-tiaoli.ts`（受控词表 v1 + DSL 解析 + 求值器）；
  `enrich.ts` 挂 `enrichment.调候条例`；`dump-text.ts` 出条例块进 chart.txt；
  `zhi-relations.ts` 仅加两处 `export`（SAN_HE/SAN_HUI 单一事实源，判定逻辑一字未动）。

  **受控词表 v1**：谓词 `透/藏/有/无/制/支/支五行/会局/成派/十神`，计数写法 `透数:丙>=2`，组合子 `且/或/非`+括号。
  词表外写法在 `npm test` 直接 FAIL。`成派` 阈值（加权分 ≥3.0 且至少一字透干）**是我方操作化定义、不是典籍原文**，
  已在 `_词表` 里带免责声明，测试断言它必须在。

  **验收**：`cd calculator && npm test` 13 项全绿（断言 261 → **279**）；`npm run bundle` 已跑，`dist-bundle/` 已更新。
  样例盘 chart.txt 见 `examples/sample-chart-jiamu.txt`（甲日，命中 6/18 条：上3 中1 忌2）；
  原 `sample-chart.json/.txt`（戊日）同步重生成，走「该格尚未吸收」的优雅分支。

  **新增的护栏（下批沿用）**：
  - **`则` 必须是本格 `原文` 的连续子串**——机器强制「层2 照录不改写」。吸收 ≠ 转述，这条最要紧。
  - `名` 必填且格内唯一——它是 J4「解读引用的条例名须在命中清单里」的唯一句柄，缺名则防编造形同虚设。
  - 条例快照锁 `EXPECTED_TIAOLI_HASH`，与取干表同规格。
  - `意象` 必填且不得等同于 `则`；chart.txt 在每条意象后原地打「概念级素材，非句式模板」的警告。

  **本批新发现（已登记，未定案）**：影印本抽查 5 格 → **3 处两系分歧**（`甲/辰` 杂气夺才 vs 混奪財神；
  `甲/丑` 庚透丁藏/丁透庚藏**高下互易**；`甲/巳` 三夏总纲先丁次庚 vs 逐月先癸後丁），一律两版并存不择一；
  另 `甲/寅` 知砚斋电子本「得丙癸**逢**」应为「**透**」之讹。待核疑点 3 → 6 条。

  **独立复核抓出并已修的四类问题（连同各自的永久测试）**：
  ① **恒真条例**——`甲申-壬无碍见戊` 写作 `有:壬 且 有:戊`，而申恒藏庚壬戊，每张七月甲木盘都命中，
  原文「须见戊土」从未被检验；同因另有 8 处合取项形同虚设。全部改按【天干透出】判 + `判定备注`，
  并新增**恒真审计**（4000 随机盘全中即 FAIL），与死条例审计一头一个堵。
  ② **一对相反论断同时打出**——`刃杀得财`[上] 与 `癸困财杀`[忌]；原文後句本是前句的例外，补 `且 无:癸`。
  ③ **ⓒ 照录护栏漏洞**——归一化把拼接标记 `／` 也抹了，跨段伪引文能过；改为按 `／` 切段逐段比。
  ④ **`细化于` 语义二义**——子集 17 例（会双计） vs 互斥 10 例（永不同时），chart.txt 却一律打「勿重复计」。
  新增 `fixtures/bless-tiaoli-relations.ts` 采样算 `关系`/`蕴含` 回写，测试复验。

  **给 M2 的交接提示**：
  - 条例走「脚本生成 → 人工復核 → 测试拦」，`则` 照录原文是硬约束。**写完必做三件事**：
    ①`npx tsx fixtures/bless-tiaoli-relations.ts` 回写关系 ②`npm test` 重新祝福 `EXPECTED_TIAOLI_HASH` ③`npm run bundle`。
  - **月令藏干两头都是坑**：寅恒藏丙戊、辰恒藏癸、巳恒藏庚、申恒藏庚壬戊、午恒藏丁、戌恒藏戊。
    典籍的「无X」照字面写成 `无:X` 会变**死条例**，「须见X」写成 `有:X` 会变**恒真条例**——一律按天干口径判并写 `判定备注`。
  - **`成派` 的五行加权含日干本身**。甲行只用到 `成派:庚辛/壬癸/戊己/丙丁`，都不含日干，无污染；
    但乙行原文有「或一派甲木」，若写成 `成派:甲乙`，日干乙会自带 1.0 分进 3.0 的阈值——**M2 开工前先定这个口径**。
  - J2（护体线升级）要用的 `病` 字段结构已就位：`{字, 依据, 出处}`，求值输出 `{字, 依据, 在盘, 透}`。
    甲行已登记 2 处病（`甲/子` 癸、`甲/申` 癸）；工单点名的 `丁/巳` 见癸透那格在 M2 的丁行。
  - 未吸收的格求值器静默返回 `有条例:false` 空壳，不抛错——M2 只需往 `条例` 块加键，无需改求值器。
- [x] **S1-1 破型重级 + S1-4 振幅分档 + 批4 三处小矛盾** ✅ 2026-07-29（M2 之后补做——按第三节的顺序本应插在 M1/M2 之间，漏了）

  - **S1-1**：区分【破型】(天克地冲/伏吟/岁运并临，进发用 −1，封顶扣一次) 与【中性型】(冲提纲/交接，只算振幅)。
    毛盘 1976 丙辰 **顺 → 平**；随包样例 `2028-2037 癸酉` 顺/剧动 → 平/剧动。`ShunNi.破型` 可追溯。
  - **S1-4**：振幅按 重4/中1/轻0.3 累加分档（<1.5 静 / 1.5~4 动 / ≥4 剧动）。**单条中级从「动」改判「静」**——
    这正是钝感的来源。毛盘 90 流年实测 静 29%→38%、动 42%→33%，三档接近均分；单条重级仍直接剧动，旧验收点不破。
  - **批4**：三条都是体检器与别处对不上，不是规则太严 →
    ①`meta.name` 的「命主」是提示词自己要求填的占位符，却被全局禁词杀掉（豁免路径入 `spec.json.forbid.all_exempt_paths`）；
    ②运岁段引用大运起止年被判「超出 5 年窗口」，可那是算法层给的事实（加大运起止年 + 建议节点白名单）；
    ③罕象提及判据是「名字前 3 字」，而「原局天克地冲」的前 3 字是「原局天」，文里写「天克地冲」反而 FAIL
    （改按 `rare.ts` 的 `匹配词` 判——什么算「提到了」由定义罕象的地方说了算，不由体检器猜前缀）。
  - 三条各锁**正反例**回归，修完不会变成放行一切。断言 291 → **310**。
- [x] **M2 乙/丙/丁 + J2 护体线升级** ✅ 2026-07-29

  **落地物**：条例扩到 **48 格 / 537 条**（甲110 乙156 丙153 丁118），`取干` 表仍一字未动。
  三行各由一个子会话独立起草（规范见 `outputs/M2-条例撰写规范.md`），我这边合并 + 机器护栏复核。

  **J2 兑现了工单点名的那个实锤点**：丁日巳月，书里明写「癸水一见，泄庚、湿甲、伤丁，故以癸为病」，
  而该格调候取【甲庚】，改动前 `癸卯` 这类干支——地支卯正对第一调候甲——护体照样加分。
  现在 `癸卯` 护体 **0.8 → −0.7（体中→体弱）**；而 `壬寅` **分文未扣**，因为
  **病按【本字】判不按五行判**——书里说癸为病的同段往往明说「壬水无碍」，拿五行一刀切会把壬一并误伤。
  参数：病透干 −1.5 / 坐支本气 −0.8 / 原局每条忌档条例 −0.5（封顶 −1.5）。
  `顺逆.典籍病忌` 把病字、忌档条例名、基线都摆出来可追溯。

  **顺带兑现 M1 复核留的前瞻提醒**：`成派` 排除日干本身。乙行原文确实有「或一派甲木」「或一派乙木」、
  丙行有「一派丙丁」——不排除的话日干白送 1.0 分进 3.0 的阈值，同一句话在比劫上会比在别的十神上更容易成立。

  **验收**：`npm test` 13 项全绿（断言 279 → **291**）；`test-shunni` 旧验收点一个没动
  （主验收盘是戊日主，戊行 M3 才吸收 → 分批吸收零副作用，这一点本身也写成了断言）。
  样例：`examples/sample-chart-dinghuo.txt`（丁日巳月，大运/流年总览里可直接看到 J2 生效）。

  **校勘**：影印本抽查累计 **30/48 格（62%）**。新增两系分歧 17 处，一律并存不择一。要紧的：
  `丁/巳`「癸水藏支」vs「無癸水暗藏」**方向相反**（⚠ 直接影响 J2 是否把藏癸计入病位，待 M5 定案）、
  `丙/子`「无戊见己」vs「多戊見己」条件正相反、`丙/巳` 富贵互易、`丁/亥`「甲乃庚之良友」主辅互易。
  另据影印本校正电子本讹字若干（丙辰「壬申两透」→「壬甲兩透」最要紧）。

  **`丁 行` 重复度疑点结案：非错**。12 格 5 种取法，但逐月条各自成文、次序随各月理由变动
  （二月「先庚後甲」是倒序、五月壬庚癸、六月甲壬庚 自成一格）——该分化处表也分化了。
  唯一例外 `丁/丑`：两本**都没有**十二月丁火专条，是文献缺口。→ **戊/己 两行的同类疑点，M3 照此办法查。**

  **给 M3 的交接提示**：
  - 流程已固化：写条例 → `npx tsx fixtures/bless-tiaoli-relations.ts` → `npm test` 重新祝福 hash → `npm run bundle`。
    **每次重跑 build 脚本都会冲掉 `关系/蕴含`，必须重新 bless**，测试会拦。
  - 日干本身在天干上，所以 `透:<日干>` 恒真、`无:<日干>` 恒假。丙行、丁行都踩过（原文「四柱多丙」「见丁比出干」）——
    一律改判 `透数:X>=2` 或 `十神透:比劫`（求值器排除日干）并留 `判定备注`。M3 的戊/己/庚 同理。
  - 受控词表**没有柱位谓词**（「时月两透」「生庚子时」「年支见子」这类）。丁行、丙行都只能近似 + 留备注。
    若 M3 觉得这类句子太多，可考虑加 `柱透:年/月/时:X` 谓词——但那是改词表版本，要单独拍板。
  - `病` 已有 4 处（甲/申癸、甲/子癸、乙/戌丙、丁/巳癸）。丙火行全篇**一个「病」字都没有**（已 grep 确认），
    这是文献事实不是漏抽；戊/己/庚 行照样只登记原文明说「为病」的。
- [ ] S2 韦千里命例试点 5~10 例
- [ ] M3 戊/己/庚 + S1-2 轴冲突
- [ ] M4 辛/壬/癸 + J3 解读接线 + J4 体检器
- [ ] S1-3 格局相神（依赖 S2 对照集）
- [ ] M5 收尾 + 发版 v3.11.0
- [ ] S3 批2（confidence 四维/引爆窗口）、批3（流月小表/财路轴）
