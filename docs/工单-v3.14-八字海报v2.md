# 工单 · v3.14 研发线 / v4.0.0 发布：八字海报 v2 确定性能力可视化

> 立项日：2026-08-01。完成日：2026-08-02。状态：**G1—G9、自动门禁、桌面/窄屏样张和作者肉眼验收全部完成；以 v4.0.0 正式发布。**
> 一句话主题：**让海报看见算法已经会的东西，并让小白无需追问就能看懂；确定性字段由渲染层直出，正向语义逐处绿色粗体。**

---

## 一、已拍板的产品取舍

- 五项能力都进入 v2，但采用“两个紧凑块 + 三个条件内联”，不把长图机械加长：
  1. 独立紧凑块：调候“典出”、裁决/轴冲突“核心看点”；
  2. 条件内联：timeline 的 `⏳`、有流月数据时的 12 月风向条、大运/流年的护体档第二维。
- 财路“经营/博弈”继续留在 `career_html`，本版不增加视觉标签。
- 保持现有 analysis schema；新结构默认全部由 `chart.json → render.ts` 投影。只有确实无法由算法字段表达的用户文案才允许新增**可选** analysis 字段，本工单默认没有这种字段。
- 保留水墨底色与命盘主体，但四类海报统一加入纸本肌理、回纹与印章红；综合/八字/紫微/MBTI 同步采用小白阅读引导和正向语义全标绿规则。

---

## 二、字段所有权与呈现契约

| 能力 | 唯一事实源 | 呈现 | 缺省行为 |
|---|---|---|---|
| 典出 | `调候条例.命中` | 按命中数组原序最多取 3 条：条例名 + 克制档位文案 + 原有意象的安全节选；不展示内部 id/判定式 | 无命中则整块隐藏 |
| 核心看点 | `出口.相神裁决`、`出口.轴冲突` | 最多 2 枚徽章；用用户语言写“为何例外/为何两面”，不露字段名和机制词 | 两者皆无则整块隐藏 |
| ⏳引爆 | 当前海报关系视图（有 `作用关系.lineage` 则用其 items，否则用 open items）+ analysis timeline 年份 | 只给已入选的五个 timeline 节点加标记；不强换节点、不添加第六项 | 无同年交集则不标，关系区仍保留窗口 |
| 流月条 | `流月引动` | 12 个节气月的顺/平/逆紧凑条 + 最顺/最逆窗口 | chart 未带流月、或年份与 render 年份不一致则隐藏并告警 |
| 护体档 | `运岁引动.顺逆[].体档` | 在大运/流年方向标记旁增加强/中/弱第二维 | 无体档时保持 v1 单维布局 |

特别约束：`振幅(静/动/剧动)` 与 `体档(强/中/弱)` 是两件事。现有 render 虽计算 `amp_label` 但模板未使用，本版不得拿振幅冒充护体档；是否另显振幅不在本工单范围。

---

## 三、执行批次

### G1 · 规格与布局定稿

1. 先修现有规格漂移：保留提示词所载的用户质量线，把 `spec.json` 从 160 提到 200，并将四大段统一为“≥6 句、≥200 字、不设上限”；同步 SKILL、bazi-poster、review，并让 `test-spec-sync` 同时核句数、字数与“不设上限”，不再只搜到“6~10”就假绿。四份现有八字金标均已超过 200 字，升级门槛不需要为样例重写而降级。
2. 在 `spec.json` 增加算法所有块的数量、截断和条件显示规则；明确 `owner=algorithm` 的块不要求提示词产出。
3. 产出版式草图：典出和看点共享一行/相邻卡片；流月条、体档嵌入现有运岁区；⏳只改节点图标。确认窄屏、长名称、三条典出和双徽章极限态不溢出。

**产物**：本工单内回填定稿说明即可，不额外制造一份会漂移的设计文档。

### G2 · 确定性渲染接线

1. `chartToFlatBazi` 生成完整可选 HTML 片段，模板只放整块占位符；空字符串即无 DOM，禁止用 `-` 占位造成空卡片。
2. 典出只读取命中条例，展示名不得来自 LLM；档位固定映射为“传统论法偏有利/中性/偏受限/提醒避开”，不写宿命断语。意象只做 HTML 转义和字符上限截断，不调用 LLM“润色”，所有 chart 字符串写入 HTML 前统一转义。
3. 核心看点使用固定白话映射，不直接透出“相神裁决/轴冲突/扶抑忌”等施工词。
4. timeline 标记在 analysis 与 chart 合并后按年份匹配；只标现有 5 项。重复命中的同年合并为一个 `⏳`，详情留在关系区。
5. 流月条要求 chart 的目标年与 `render --currentYear` 一致；不一致时隐藏并向 stderr 输出一次 warning，绝不把旧年数据标成今年。
6. 护体档读取算法层现成 `体档`；方向与护体用形状/文字双编码，不能只靠红绿，兼顾灰阶与色觉差异。

### G3 · 门禁、兼容与视觉验收

1. `check-template` 增加五项必需整块占位符与标签平衡检查；最终 HTML 的残留占位符由 `test-golden` 检查。后者还要明确断言每个条件块的出现/隐藏，不只检查 HTML 能打开。
2. `check-analysis` 保留现有条例引用防编造，因新区块由算法生成，不新增同名 LLM 字段与重复校验。
3. 兼容矩阵：
   - 旧 analysis + 新 chart + 新模板可渲染；
   - 新模板遇旧 chart（无新字段）与无 analysis 均不崩；既有叙事字段沿用 v1 缺省行为，五个新增可选块不得留下空 wrapper；
   - 有/无 `--currentYear`、年份匹配/不匹配均有正反例；
   - 切换 `--lineage` 不改变 open 中立 `作用关系.items/policy_note`；⏳与关系表可共同切换到所选流派视图，二者必须始终同源。
4. 特性矩阵至少三盘：1991 质检盘（轴冲突/条例/流月）、蒋或吴盘（相神裁决）、含半局引爆窗口的公开或合成盘。单一“全能盘”不得代替正反例。
5. 自动门禁查 DOM、字段与溢出；另生成桌面宽度和窄屏截图供肉眼验收，截图放 `_scratch` 或临时产物，不把跨平台像素图当稳定金标提交。

### G4 · 四类海报小白化与正向语义全覆盖（用户追加）

1. 四类模板统一加入纸本肌理、回纹、印章红、小白阅读说明与窄屏适配；章节名改成直接对应现实问题的白话标题。
2. 四份提示词与 SKILL 同步要求：优势、能力、好性格、适配方向、助力、顺风/上行窗口、改善与收益逐处 `hl-good`，不是每段选一两句；正负混合句拆开着色。
3. 八字四大段每段至少 3 处绿色仅作机器底线；综合主轴/阶段段、紫微与 MBTI 各段另设最低存在门。体检器先移除已标绿内容，再扫描剩余普通正文的高置信正向词，漏标即 FAIL。
4. 四份随包 analysis 和最终 HTML 样例全部重制，确保用户看到的示例本身也体现高密度绿色粗体，而不是只更新后台规则。
5. 十神修复早期“一句话”遗漏：机制段恰好 1 句，白话段升级为 ≥6 句、≥200 字、不设上限，并完整回答能力与内在需要、决策与协作、工作落点、发挥收益、过度代价和行动建议。
6. 新增独立“罕见现象”板块：算法层负责罕象名称、罕见度、涉及柱；解读层逐项给出“偏正向／偏提醒／两面性”、现实表现和建议。罕见不等于吉或凶，无罕象时零 DOM。

### G5 · 结论前置与渐进披露（用户二次反馈，已由 G6 扩展）

1. 上一版曾使用性格、事业、关系、身心、当下五关键词总领（每项 4~8 字、两句总领≤110字）；该形态已被 G6 的八项坐标取代，仅保留为迭代记录。
2. 性格、事业、感情、健康、变化、近年、关键阶段与行动建议整体前置，并用低饱和主题色帮助扫读；不能用鲜艳多色制造新的视觉负担。
3. 出生资料、完整四柱详表、旺衰、五行、十神、格局用神、大运流年、神煞与 MBTI 映射统一放在“专业依据”分隔线之后。G7 追加的顶部四柱核心条只显示四柱干支与日主；旧 analysis 缺 `overview` 时仍显示该身份锚点，只隐藏八项坐标。
4. 自动门禁锁住 `overview → interp.personality → evidence-divider → 四柱 → 十神` 的 DOM 顺序；桌面与 390px 窄屏重新截图验收。

### G6 · 八项坐标与“水纹裁光”视觉重构（用户三次反馈）

1. 总领由五个短标签扩为 01~08 八项人生坐标：01~04 为性格、事业、关系、身心主坐标，05~08 为变化、年份、阶段、行动推进坐标；每项 3~4 个短词、每词 2~8 字。
2. 总领卡与正文同编号章节必须共享精确标题、色系、汉字水印和内容语义，并用锚点一一链接。旧五项 overview 不渲染半套新版总领，但整份旧海报仍可生成。
3. 总领扩大空间但保持清透：使用骨白宣纸与极浅五行晕染，撤掉深蓝整块和点阵麻纹。主题按日主木火土金水自动切换，作用于大面积浅洗色、细线、圆环、编号与边框；每个现实色块使用透明渐变、浅水纹和低透明汉字水印，不铺纯色。
4. 视觉元素限制为水纹、开口圆环、细刻度、编号和水印字；禁止点阵、颗粒噪声、仿麻布纹理，也不得追加仿古花边或神秘主义符号。间距、颜色、透明度和响应式规则统一记录在 `references/bazi-poster-visual-system.md`。
5. 自动门禁新增八坐标字段完整性、每项短词数量、专业术语禁入、01~08 唯一锚点、总领先于阅读指引、视觉令牌存在与 1440px/390px 无溢出检查。

**内部验收状态（2026-08-02）**：实现、样例重渲染、桌面/390px 截图、五行主题路由、构建与 18 项全回归均已通过；作者已完成肉眼确认，发布版本定为 v4.0.0。

### G7 · 四柱身份锚点与主题权重修复（用户四次反馈）

1. 标题后总领最上方增加算法直出的四柱核心条，依次显示年柱、月柱、日柱、时柱，并使用日主主题突出日柱与“日主 X五行”。这是八字海报的身份锚点，必须先于八项人生坐标出现。
2. 核心条只显示四柱干支与日主，不显示藏干、十神、星运、自坐、纳音、神煞或旺衰；完整四柱详表仍位于“专业依据”之后，避免第一屏重新过载。
3. 修复日主路由正确但总领仍偏绿的问题：移除写死的月白青绿主表面，背景、卡片浅洗色和摘要重点统一从 `--theme-wash-rgb` / `--theme-rgb` 派生。正文绿色正向语义继续保留，但不得主导总领整体色相。
4. 旧 analysis 或无 analysis 时，四柱核心条仍由 chart 独立渲染；不完整的旧五项 overview 继续隐藏，不生成半套 01~08 坐标。
5. 自动门禁新增“四柱核心条恰四柱、日柱突出、核心条先于八坐标、固定青绿底色不得回归”检查；桌面与 390px 均须重截图验收。

### G8 · 06 年份节奏白话化（用户五次反馈）

1. 章节名由“哪些年份更有感”改为“未来几年怎么走”，总领 06 与正文同步改名，继续保持编号、标题、色系、水印与锚点一一对应。
2. 主区不再铺终身大运与合冲刑害技术行。渲染层从未来五年流年信号和建议节点中筛选最多 4 个关键年份，按时间排序，每张卡只显示年份/干支、白话节奏和一条行动建议；样例固定呈现 2026“变化最明显”、2027“适合收整”、2028“进入换挡期”。
3. 主区明确写“没有列出的年份不代表不好，只是变化感相对较弱”；变化强度与方向不混为吉凶，朱砂仅作收整提醒的细线坐标，不使用灾年式文案。
4. `yunsui.reading_html` 保持既有 3~7 句机器区间，但提示词优先 3~5 句、最多解释 3 个节点，先总节奏再给行动，不重复专业术语清单。完整 `yunsui.rows_html` 后置到大运/流年专业依据中的折叠区，数据不删。
5. 自动门禁锁住样例三卡年份、白话标题、主区零 `hc-row`/零专业术语、后置明细仍存在；桌面与 390px 截图同时确认三卡等分/单列、零横向溢出。

### G9 · 统一字级与长文分段（用户六次反馈）

1. 01~08 总领卡合并到同一个等轨网格，八张卡使用完全一致的尺寸、内边距、标题字号、关键词字号、链接和水印尺度；删除 05~08 的“次轨缩小”规则。
2. 建立统一字级令牌：总领卡标题、现实章节标题和专业面板标题都必须比相邻正文更大且更粗；移动端可以同比缩小，但不能倒置层级。
3. 渲染层将四大现实解读、三个精读段、十神白话与相关说明按完整句义自动整理为每 2~3 句一个段落，并在内联高亮闭合后再切段，避免破坏绿/红语义标签。
4. 提示词与评审只负责把每 2~3 句写成一个微主题，不允许模型直接输出块级标签；自动门禁检查八张卡同源样式、标题字号关系、段落数量及标签平衡。
5. 桌面与 390px 重新截图，重点验收八卡等规格、标题层级、01~04 与 05~06 的段落呼吸和页面零横向溢出。

---

## 四、发布门

- 五项能力各有“出现 + 隐藏”断言；旧 analysis、新旧 chart 兼容矩阵全绿。
- 页面无 `{{...}}`、`undefined/null`、空标题、空卡片；窄屏无横向溢出，长图分页/截图不断块。
- `npm test` 全绿；源码与 dist-bundle 渲染结果一致；示例 analysis 不因无新字段而重制，只有样例 chart/HTML 因确定性新区块按需刷新。
- 作者已通过特性样张，并确认以 v4.0.0 发布。

## 五、明确不做

- 不新增破坏性的 analysis schema v2；`rare.reading_html` 与 `overview` 都保持渲染层向后兼容（旧 analysis 缺字段时相应整块隐藏），新生成稿按提示词要求齐全。不让 LLM 生成罕象名称/罕见度、条例名、裁决类型、引爆年、流月方向或体档。
- 不显示 confidence 内部机制；只让既有分维规则影响措辞。
- 不加财路性质视觉标签；不做身弱侧降忌权；紫微十二宫与其他三类海报不在本轮重排。八字四柱数据不删除、不改写：顶部只增加四柱核心速览，完整详表继续后置到专业依据区。

## 六、执行记录

- [x] 2026-08-01 作者拍板：与旺衰拆版；五项条件化呈现；财路不加标签；F4 延后。
- [x] G1 规格与布局定稿：四大段统一为 ≥6 句、≥200 字、不设上限；五块所有权/数量/截断/条件规则进入 `spec.json`，同步门禁不再假绿；双卡桌面并排、窄屏堆叠定稿。
- [x] G2 确定性渲染接线：五项均由 `chartToFlatBazi`/合并后 timeline 注入；空值零 DOM；流月错年只告警一次；lineage/open 关系表与 ⏳ 同源；chart 字符串统一转义。
- [x] G3 门禁、兼容与视觉验收：`test-poster-v2` 覆盖五项正反例、新旧 chart/analysis、无 analysis、年份与 source↔dist；1991 质检盘、蒋例、lineage 引爆合成盘各有 1440px/390px 截图（工作区 `../_scratch/v3.14-bazi-poster-v2/`），6/6 页面无横向溢出、空卡或残留占位符。为压力测试长文版式，特性盘的叙事段沿用随包 analysis，仅验确定性区块与布局，不把叙事内容当命例金标；作者已完成样张验收。
- [x] G4 四类海报小白化与正向语义全覆盖：提示词、评审、SKILL、体检器、模板阅读说明、随包 analysis/HTML 样例与文档同步；已有绿色但仍漏标普通正向句子的回归用例已加入；十神扩为完整长段，新增条件化“罕见现象”板块及其逐项性质/现实落点门禁。
- [x] G5 结论前置与渐进披露：现实维度前置、专业依据后置、旧 analysis 空块兼容与 DOM 顺序门禁已落地；其五标签总领已由 G6 八项坐标取代。
- [x] G6 八项坐标与“水纹裁光”视觉重构：字段、渲染、模板、视觉令牌和自动门禁已落地；桌面/窄屏样张完成精修并经作者确认。
- [x] G7 四柱身份锚点与主题权重修复：渲染、模板、兼容策略、文档、桌面/390px 样张与自动门禁均已落地；构建、bundle 与 18 项完整回归通过。
- [x] G8 06 年份节奏白话化：主区改为 3~4 张年份/节奏/建议卡，终身技术行后置折叠；提示词、评审、样例、文档、桌面/390px 样张与自动门禁均同步完成。
- [x] G9 统一字级与长文分段：01~08 改为单一等轨网格并移除后四卡缩小分支；标题字级全面高于正文；四大解读、三个精读段、十神与相关说明按完整句义自动分段。桌面八卡实测统一为 235×167，390px 下统一为 160×186；两端均无横向溢出，构建、bundle 与 18 项完整回归通过。
- [x] v4.0.0 发版
