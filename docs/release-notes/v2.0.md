# bazi-ziwei → v2 变更摘要

> 产出位置：`Bazi-Ziwei-Decoder/`（旧 skill 未原地改动）。算法层中立、流派只做解读镜片、神煞只增色不定大局、八字分析开头必出成长心态声明——四条底线全程遵守。

## 测试结果（唯一对错信号，全过）
- `calculator/schema-check.ts` → **exit 0**（24 神煞 / 6 流派 / 10 引擎 method 一致）
- `calculator/fixtures/test-shensha.ts` → **7 例全绿 exit 0**
- 端到端冒烟：真实生辰 → `run-chart` → `dump-text` 通；`--lineage` 切换时四柱/十神/大运不变、神煞镜片随派变（子平/open=全集、滴天髓≈空亡+羊刃、盲派只核心象、段氏 stub→0 安全跳过）。
- `npx tsx` 与编译后 `node dist/` 两条路径均跑通（dist 已重新编译）。

## 工单完成情况
| 工单 | 状态 | 说明 |
|---|---|---|
| T0 落位 | ✅ | handoff 落入 `calculator/`（shensha.json/ts、lineages.json、schema-check.ts、fixtures/）与 `prompts/`（disclaimer-preamble、output-mode-B）。布局即对齐相对 import，无需改路径。 |
| T1 神煞进算法层 | ✅ | `run-chart.ts` 算完 bazi 调 `computeShensha` 写 `bazi.enrichment.神煞`（`hits`=中立全集；传 `--lineage` 附 `lineage.hits`=镜片子集）。`dump-text.ts` 八字段加「神煞」块（柱位 + tier + via + ⚠起法待核）。 |
| T2 流派进解读层 | ✅ | SKILL.md Step0 加选流派 + 输出形态；`run-chart.ts` 加 `--lineage`（仅出文镜片，**不改排盘**）；`bazi-prompt.md` 四支柱骨架改 **五行→十神→神煞→大运**，注入 `{{lineage.prompt_inject}}`/`{{lineage.pillar_emphasis}}` + 神煞铁律；盲派/段氏「五行」支柱改述干支象与做功。 |
| T2.5 输出模式B | ✅ | `bazi-prompt.md` 接 output-mode-B：首回合【总领速览】（顶部告示 + 定调锚点 + 固定章号菜单 + 底部下钻提示），后续「详细展开第X章」只深写该章 + 顶部复述锚点 + 进度条；不自动逐章、不诱导继续。Step0 加「总领速览(默认)/完整报告(Cowork)」。 |
| T3 成长心态 disclaimer | ✅ | `disclaimer-preamble.md` 接到八字分析**最开头**（命盘解读之前），首回合必出全文；下钻回合用一行锚点复述代替。 |
| T4 消化 needs_review | ◑ | 见下表。凭文献核验，1 项消化 + 多项确认/记录，4 项保留待定（绝未凭记忆改 false）。 |
| T5 段氏 stub | ✅ | `duanshi_TODO` 各 `_TODO` 原样保留；引擎对 stub（字符串权重/whitelist_TODO）安全跳过 → 段氏镜片 0 命中、schema-check 仍过。**未编造任何段氏权重/象解。** |
| T6 八字海报（可选） | ⏸ 未做 | 标记为可选，本轮未实现（如需再开）。 |

## T4 · needs_review 逐项（凭《三命通会》/《渊海子平》，神峰为扫描图无可提取文本）
| 神煞 | 处理 | 文献依据 |
|---|---|---|
| 红艳煞 | ✅ **改 false** | 《三命通会·卷三》桃花红艳煞条原文「甲乙午、丙寅、丁未、**戊子**、己辰、庚戌、辛酉、**壬巳**、癸申」与本表逐位一致（本 skill 取三命通会版，非通行戊辰/壬子版）。 |
| 魁罡·戊辰 | ✅ 确认默认关 | 《三命通会·卷六》日魁罡列 庚辰/庚戌/壬辰/戊戌 四日，戊辰不在内（戊辰戊戌另作「魁罡相会」）。`table_variant_optional` 默认关闭=正确。 |
| 羊刃·阴干刃 | ✅ 确认默认关 | 《三命通会·3.13》明言「子平以甲丙戊庚壬五阳干有刃，乙丁己辛癸五阴干无刃」。`table_yin_optional` 默认关闭=正确。 |
| 三奇·顺序 | ◑ 记录待定 | 《三命通会·卷三·论三奇》重「顺布连珠/上局相生生复生」。引擎现按**集合相等**收，**严格次第未启用** → 待你决定是否启用严格顺序。 |
| 元辰·边界 | 🔴 保留待定 | 核心起法已核验（《三命通会·3.15》阳男阴女冲前一位/阴男阳女冲后一位，引擎一致）；但原文「余干**午未半之**」边界未实现 → 待你确认边界处理。 |
| 文昌贵人 | 🔴 保留待定 | 本表=通行「食神临官」版；《三命通会·卷三》另载「文昌贵」诀（甲巳乙亥丙戌丁辰戊申己午庚寅辛未壬卯癸丑），且另有文星贵/文誉贵 → **两版分歧，待你定版**。 |
| 福星贵人 | 🔴 保留待定 | 本表=通行「日干→支」版；《三命通会·6.67》作「甲丙相邀入虎乡」遁真食神法（甲见丙寅/丙子、乙见丁亥/丁丑…，以年论）方法不同 → **待你定版**。 |
| 国印贵人 | 🔴 保留待定 | 已核验《三命通会》《渊海子平》**均无此煞**；属后世《协纪辨方》系 → 待你确认是否纳入及起法。 |
| 三合起算基准 | ◑ 待定（政策） | 引擎默认 `config.sanhe_seed=["年","日"]`（年+日并查）；古法仅年支 → 待你定政策（改 `["年"]` 即复古）。 |

> 第二轮按你的 6 项决定，余下 4 项全部消化，现 **`needs_review: true` = 0**（见下节）。

## 第二轮：你的 6 项决定已落地（needs_review 归零）
| # | 你的决定 | 实现 | 文献/测试 |
|---|---|---|---|
| 1 文昌 | 通行版优先 + 算一遍三命通会、古法没有就提示 | 通行「食神临官」为命中主表；新增**古法交叉校验**：命中时另算《三命通会》文昌贵，古法无则注「古法无」、亦合则注「古法亦合@柱」。`needs_review→false` | 古法甲乙句原文「甲乙蛇口乙猪头」字句残损 → 列 `unverified_keys`，标「未校验」**不臆测**（铁律）。丙–癸古法已编码 |
| 2 福星 | 同上 | 通行「日干→支」为主表 + 古法交叉校验（《三命通会》6.67 年干遁食神）。`needs_review→false` | 6.67 原文仅列甲(丙寅/丙子)、乙(丁亥/丁丑)，「余倒推」未列全 → 丙–癸列「未校验」不补全 |
| 3 国印 | 纳入 | `needs_review→false`，保留通行国印表，命中生效（已测@日柱） | 注明三命通会/渊海无载、属《协纪辨方》系，起法以通行表为准 |
| 4 元辰 | 实现 | 核心起法（阳男阴女冲前一位/阴男阳女冲后一位）已实现并验证，全 12 支表正确。`needs_review→false` | 《三命通会》3.15；「午未半之」经核读为对子/丑示例(落未/午)的描述性说明、非独立算法，已注明，如另有特定算法可再补 |
| 5 三奇 | 启用严格顺布 | `sanqi` 由集合相等改为**严格顺布连珠**（连续三柱恰为该奇顺序；倒乱不作奇） | 《三命通会》卷三论三奇；新增 fixtures c8(乱序不作奇)/c9(顺序@月日时) 全绿 |
| 6 三合 seed | 年+日并查 | 维持 `config.sanhe_seed=["年","日"]`，`_meta` 标注为**正式政策** | 古法仅年支；如需复古改 `["年"]` 即可 |

**第二轮测试**：`schema-check` exit 0；`test-shensha` **9 例全绿**（含新增三奇 2 例）；古法交叉校验、国印命中、needs_review=0 均冒烟验证通过；dist 已重编译。

> 古法「未校验」项（文昌甲乙、福星丙–癸）是因为这两处《三命通会》原文字句残损/「余倒推」未列全 —— 据铁律保留为「未校验」而非编造。若你有这几处的可靠文献，我再补全古法表。
