# 更新日志 · Changelog

> 项目：**Bazi-Ziwei-Decoder**（八字紫微命理基板）
> 版本号遵循 [语义化版本 SemVer](https://semver.org/lang/zh-CN/)；格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)。
> 基于 [dzcmemory-web / bazi-ziwei-skill](https://github.com/dzcmemory-web/bazi-ziwei-skill)（MIT，排盘内核源自 [Yiqi](https://github.com/fdxuyq/Yiqi-BaZi-ZiWei)）**超级魔改**而来。

---

## [Unreleased] · v3.11.0 开发中 · 造化元钥吸收工程
> 工单:`docs/工单-v3.11-造化元钥吸收.md`。分 M1~M5 批走,每批独立验收。

### Security 安全收敛 · 移除自动更新的远程代码执行链
> 起因:外部模型/扫描器反复标记本仓库。全仓复扫后确认**只有两处**触发点,均已处理;
> 代码本体干净——无 `eval`/`new Function`、无 base64 混淆、无凭证、vendored 的 `yiqi-core` 零网络调用。

- **`self-update.ts` → `version-check.ts`,砍掉下载执行链（真问题,不是误报）**。
  旧版做的是「影子更新」:开机静默 GET 仓库 zip → `execFileSync('unzip')` 解压落地 →
  `SKILL.md` 指示模型**把 `<skill-root>` 切到刚下载的目录**,后续命令改跑那份新下载的 `dist-bundle/*.js`。
  这等于**每次会话自动从网络拉取代码并执行**——即使拉的是作者自己的仓库,这个**形状**就是
  远程代码执行 / 供应链风险的教科书样本,被标记是应该的。现改为**只读版本比对**:
  - 唯一网络行为是 GET 一个纯文本 `VERSION`(限 64KB、重定向 ≤3 跳);
  - **不写任何文件、不起子进程、不解压、不改变 `<skill-root>`**;发现新版只提示用户手动重装 `.skill`;
  - 移除 `--fetch` / `--workdir` 参数与 `child_process` 依赖;`SKILL.md` 启动章节同步重写并明写「本 skill 不做自动更新」。
- **`lineages.json` 的 `prompt_inject` 字段更名为 `pai_panci`**(纯命名撞词,零功能风险)。
  该字段的实义是「本派判词,作为解读总纲」,与 prompt injection 毫无关系,但字面正是那个安全术语,
  且全仓出现 11 次(含 `prompts/bazi-prompt.md`、`SKILL.md`),与上一条叠加会构成「可疑项聚集」的观感。
  **该字段从不被任何 `.ts` 读取**(由 LLM 直接读 JSON),故改名为纯文本替换,零行为变化。
  CHANGELOG 与 release-notes 里的历史提及保留原名并就地注明更名,以存史。
- **验收**:`npm test` 13 项全绿(断言数不变,323);`npm run bundle` 已重跑,
  `dist-bundle/self-update.js` 已删除、`dist-bundle/version-check.js` 已生成(产物内 `child_process`/`codeload`/`unzip` 出现次数 = 0)。

### Added 新增 · M4 · 辛/壬/癸 三行（十干收官）+ J3 解读接线 + J4 防编造
- **全表 120 格 / 1396 条条例吸收完毕**（甲110 乙156 丙153 丁118 戊146 己158 庚127 辛158 壬176 癸94）。
  `取干` 表自始至终一字未动，原快照锁全程未受影响。至此每一格都从「取干两个字」变成了一棵**可对盘面求值的条件树**。
- **J3 解读接线**：`bazi-prompt.md` 加「调候条例」四条硬约束 ——
  ① **只能引用命中清单里的条例名**（未命中的一个字都不许出现，命中 0 条时不得引用任何条例名）；
  ② 档位（上/中/下/忌）是书的判断不是实证，必须转写为倾向性表述，禁止写成宿命断语；
  ③ **反同质化（最要紧）**：`意象` 是**概念级素材不是句式模板**，同一条例落到不同盘上必须结合**本盘的格局/十神/大运语境**重写 ——
  「寒木向阳」在食伤旺的盘里可以讲创作端的舒展、在官杀重的盘里可以讲被赏识的通道；**严禁原句照抄，严禁给任何条例配固定白话句式**
  （v3.7.1 的正缘画像就是这么写成全盘同款文案的）；
  ④ 带 `⚖两系分歧` 的不得只引一方，带 `细化于…子集` 的与母条讲的是同一件事、别当两件事各说一遍。
  海报侧不新增字段（那会破坏金标样例与模板），约束挂到既有的 `yongshen.note_html`。
- **J4 防编造**：`check-analysis` 新增 `_条例引用` 项 —— 引用**未命中**的条例名即 FAIL。
  这是 J3 给解读层的自由必须配的那把锁:**措辞可以自由，事实不行**。
  判据分两层：本格的未命中名（≥2 字全查）+ 别格的名（≥4 字才查 —— 「寒木向阳」这类足够独特，
  而「假从」「壬透」这种 2~3 字的在正常行文里会撞车）。零误伤已锁进回归。
- **`bless-tiaoli-relations` 采样数 40000 → 12000**：全表 120 格后 40000/格 = 480 万次求值，
  祝福脚本与测试 ⓖ 都跑不动（实测被杀）。关系判定只需命中集合的包含关系，12000 足够稳，且测试用同一 seed 复验。
- 分批期间那条「未吸收的格静默返回空壳」断言**没有删掉**，改成了两条:①全表 120 格齐全（M4 的验收标准本身）
  ②空壳分支仍然健在（拿一个不存在的格直接验）—— 那条分支是活代码（日后加流派镜片、换底本、撤格都会走到），
  不该因为「碰巧全吸收完了」就失去保护。

### Added 新增 · M3 · 戊/己/庚 三行 + S1-2 轴冲突标记
- **条例扩到 84 格 / 968 条**（甲110 乙156 丙153 丁118 戊146 己158 庚127），十干里已吸收七干。`取干` 表仍一字未动。
  影印本抽查累计 **48/84 格（57%）**。三行仍是各起一个子会话按 `docs/条例撰写规范.md` 起草、主会话合并 + 机器护栏复核。
- **S1-2 轴冲突显式标记**。毛盘回测的第三条:该盘调候取甲庚（木金）而扶抑忌金 —— **庚金年「调候说该来、扶抑说别来」**。
  改动前两轴各说各的、谁也不提对方，解读层只能读成两个独立信号，写出来就是「宜金」和「忌金」并列出现，自相矛盾还看不出为什么。
  现在 `出口.轴冲突` 把交集算出来摆在明处（调候侧说什么 / 扶抑侧说什么 / 出文要求），chart.txt 打成独立块，
  `bazi-prompt.md` 加硬约束:**必须合并成一句讲清这个五行的两面性，严禁在同一篇里既写「宜X」又写「忌X」而不点破二者是同一件事**。
  交集为空时不生成该字段 —— 免得解读层被无谓的告警噪音淹掉（这一条也写成了断言）。
- **`戊 行`/`己 行` 重复度疑点结案:非错**（丁行 M2 已结，至此三条全清，`校勘.待核疑点` 里再无「重复度」类）。
  戊行的判据是结构性的:**戊行只有「三春戊土」一段总论**，四月起到十二月根本没有三夏/三秋/三冬总纲 ——
  「套用总纲」在九个格上不可能，没有可套的纲。且总论自身就分月（「正二月先丙後甲…**三月先甲後丙**…因戊土司权故也」，
  影印本书页 245 独立作「正二月宜先透丙，三月宜先透甲」，两系同证）。
  逐月各自带理由且次序随理由变动:五月「仲夏火炎，先看壬水…**用癸力微**」、八月「**不必木疏**」（明文排除甲，是主动取舍非省略）、
  九月「不可专用丙…见金先用癸水，後取丙火」（条件性改序）。
  **「仅 4 种取法」是把有序三元组归并成无序集合后的统计假象** —— 按字面次序戊行实为 8 种。
- **机器护栏在本批抓出 3 处**（都是子会话自审没发现的）:己/寅「配得中和」要求庚癸丙三透，加日干己已占满四个天干位，
  `成派:甲乙` 的「须透干」再无位置 → 结构性死条例；己/丑「不见比劫」按含藏干判，而丑本气即己 → 恒假；
  还有一条 `非 透:` 改判缺 `判定备注`。**都是 4000 随机盘审计当场逮住的。**
- 顺带修了 ⓕ「改判须留痕」判据的一处误伤:原来只看「则里有没有『无』」，
  于是「丙藏衣禄**无亏**」这种被当成口径调整。改为精确到**是哪个字的「无」**（`无X`/`不见X`/`全无`）。

### Fixed 修复 · S1-1 / S1-4 / 批4 —— 毛盘回测暴露的两个计分问题 + 体检器三处自相矛盾
> 这几条本该插在 M1 与 M2 之间（工单第三节的执行顺序），漏了，现补上。

- **S1-1 破型重级进方向**。v3.10 把方向与振幅正交化时，把「破型重级」一并从方向里拿掉了——
  毛盘回测的实锤:**1976 岁运并临，振幅判「剧动」而方向仍判「顺」**，把一个明确的破局年说成顺风。
  现在区分两类重级:【破型】天克地冲/伏吟/岁运并临 = 结构性破 → **进发用线 −1（封顶扣一次，两条破型不等于两倍逆）**；
  【中性型】冲提纲/大运交接 = 只是动得大 → 仍不进方向，正交在这一侧继续成立。
  实测:毛盘 1976 丙辰 **顺 → 平**；随包样例 `2028-2037 癸酉` **顺/剧动 → 平/剧动**（天克地冲）、
  丁日样例 `2037-2046 丁亥` **平/剧动 → 逆/剧动**。`ShunNi.破型` 字段可追溯。
- **S1-4 振幅分档改累加**。原口径取最重的一条，于是任一「中」级引动（支冲/自刑/相刑/凑局…）就算「动」——
  毛盘 90 个流年里「静」只剩 29%，这根轴基本没有区分力，大部分年份看着都在动。
  改为按 重4 / 中1 / 轻0.3 累加分档（<1.5 静 / 1.5~4 动 / ≥4 剧动）:**单条中级仍算静，两条中级才算动**；
  单条重级=4 直接剧动，与原口径一致故回归不破。实测毛盘 **静 29%→38%、动 42%→33%**，三档接近均分。
- **批4 · 体检器三处内部矛盾**（都不是「规则太严」，是体检器和别处对不上）:
  - **`meta.name` 的「命主」被当播报腔杀掉**。`bazi-poster.md` 明写「name:没提供填『命主』」，
    而 `spec.json.forbid.all` 又把「命主」列为全局禁词——**模型照提示词写必被打回，照体检器写又违反提示词**。
    加 `forbid.all_exempt_paths`（豁免路径写在 spec.json，单一事实源），只豁免姓名槽位；正文里的播报腔照拦。
  - **运岁段引用大运年份被报警**。一步大运横跨十年，「2028-2037」必然落在「今年起 5 年窗口」外——
    可那是算法层自己给的事实。豁免大运起止年 + 建议节点年；白名单外的无关年份（如 1949）仍报警。
  - **罕象提及判据是「名字前 3 字」**。「原局天克地冲」的前 3 字是「原局天」，而文里自然写的是「天克地冲」，
    于是模型明明点名了罕象照样 FAIL。改为按罕象自带的 `匹配词` 判（`rare.ts` 定义处说了算，不由体检器猜前缀），
    缺省回退到「去括号、去『原局』前缀的全名」。
- 三条各自锁了**正反例**回归（`test-shunni.ts` / `test-check.ts`），修完不会变成放行一切。断言 291 → **310**。

### Added 新增 · M2 · 乙/丙/丁 三行 + J2 护体线接典籍病忌
- **条例扩到 48 格 / 537 条**(甲 110 / 乙 156 / 丙 153 / 丁 118),覆盖十干中的四干。`取干` 表仍一字未动。
  档位 上134 / 中194 / 下105 / 忌104;带 `判定备注` 182 条、内联 `两系分歧` 46 条、格级 `两系分歧` 20 处。
- **J2 · 护体线第一次有典籍级依据**。此前护体只加不减:丁日主生巳月,《穷通宝鉴》明写
  「四月丁火乘旺…但四柱忌见癸水,**癸水一见,泄庚、湿甲、伤丁,故以癸为病**」,而该格调候取【甲庚】,
  于是癸卯这类干支——地支卯正对第一调候甲——护体照样加分,癸的伤害在计分里根本不存在。**计分与典籍相悖的实锤点。**
  现在:病字透干 −1.5 / 坐支本气 −0.8,原局每命中一条「忌」档条例 −0.5(封顶 −1.5)。
  实测该盘 `癸卯` 护体 **0.8 → −0.7(体中→体弱)**,而 `壬寅` **分文未扣**——因为
  **病按【本字】判不按五行判**:书里说癸为病的同段往往明说「壬水无碍」(甲/申「壬水无碍,且能合丁」),
  拿五行一刀切会把壬一并误伤,那就不是典籍的意思了。`顺逆.典籍病忌` 把病字、忌档条例名、基线摆到台面上可追溯。
- **`成派` 排除日干本身**(M1 复核时预警,M2 兑现)。「一派」说的是日主**周围**的势,日主自己是被论的那个,不是「派」的一员。
  甲行只用到 `成派:庚辛/壬癸/戊己/丙丁`,不含日干,无差别;但乙行原文有「或一派甲木」「或一派乙木」、丙行有「一派丙丁」——
  不排除的话日干白送 1.0 分进 3.0 的阈值,**同一句话在比劫上会比在别的十神上更容易成立**。
- **「若」的语法树缓存**:同一条条例被上万张盘反复求值(死条例/恒真/关系三项审计),每次重新分词建树纯属浪费。
  加 AST 缓存后 19.2 万次求值 1.8 秒,测试从超时变成能跑完。
- `examples/sample-chart-dinghuo.txt`:丁日巳月样例盘(合成 1980-05-14 12:00 男 → 庚申 辛巳 丁亥 丙午),
  chart.txt 的大运/流年总览里可直接看到 J2 生效(1997-2006 癸未 体−1.5 / 2023 癸卯 体−0.7 / 2022 壬寅 体0.8 未扣)。

### 校勘 · M2
- **影印本抽查累计 30/48 格(62%)**:乙行 7 格、丙行 6 格、丁行 7 格,各含本行「条例最多的格」并跨四季。
- **新增两系分歧 17 处**,一律两版并存不择一。要紧的几条:
  - `丁/巳`:知砚斋「或癸水**藏支**,壬水出干制丙」vs 影印本「或**無**癸水暗藏」——**方向相反**。
    影印读法与同段「故以癸为病」自洽,李铁笔語譯亦作「未見癸水暗藏於支」。⚠**此分歧直接影响 J2**:若从影印本,藏癸也当计入病位。已标注待 M5 定案。
  - `丙/子`:「**无**戊见己,异路功名」vs「**多**戊見己,不過秀才」——条件正相反。
  - `丙/巳`:「不富必**贵**…但心性乖僻」vs「不**貴**而**富**多…出眾超羣」——富贵互易且褒贬相反(性质同 M1 的甲/丑)。
  - `丁/亥`:「**甲**乃**庚**之良友」vs「**庚**乃**甲**之良友」——主辅互易。
  - `乙/亥`:「有**丙**无**戊**」vs「有**戊**無**丙**」——两字恰好互易。
- **据影印本校正知砚斋电子本讹字若干**:丙/辰「壬**申**两透」→「壬**甲**兩透」(申是地支,不当与壬并称「两透」,且上下文皆言甲)、
  丙/寅「**篌**里藏刀→笑」「得**康**金→庚」、乙/辰 补脱字「丙癸全**無**」、丁/子「**金**无比印」→「**全**無」。均在各格 `影印本抽查.记录` 留痕。
- **`丁 行` 重复度疑点结案:非错**。丁行 12 格只有 5 种取法,但逐月条各自成文、各自推出甲庚,且**次序随各月理由变动**
  (正月甲庚 / 二月「湿乙伤丁,**先庚後甲**」倒序 / 三月「**先用甲木**引丁制土,次看庚金」),五月壬庚癸、六月甲壬庚 又自成一格——
  该分化处表也分化了,不是复制粘贴。且书本身按季给统一取法(「三冬丁火微寒,專用甲庚」)。**重复是底本的写法,不是抽取错误。**
  唯一例外 `丁/丑`:知砚斋本与影印本**都没有十二月丁火专条**,只能由三冬总纲覆盖——文献缺口,非抽取遗漏。
- **新增疑点 2 条**:①`丁/寅` 那条已核引文「非庚不能劈甲,非甲不能引丁」在知砚斋正月条里不是原样
  (正月作「非庚不能劈甲,**何以**引丁,姑用庚金」),最接近的是影印本**三冬**丁火总纲——引文可能系错了格,取干甲庚不受影响,待 M5 定出处。
  ②`乙/丑`、`丁/丑` 的篇目缺口(见上)。

### Added 新增 · M2 · 测试
- `test-tiaohou.ts`:新增「条例块的 `取干` 必须与取干表逐字一致」——两处记同一件事就会漂移,条例又是分批分人写的。
  未吸收样本从 乙/寅 换成 戊/寅(乙行本批已吸收)。全套断言 279 → **291**。
- `test-shunni.ts`:新增 J2 十一项断言,包含**反例**——`壬寅` 必须分文未扣(证明病按本字不按五行)、
  `庚寅` 与病无涉护体不变(证明 J2 不是全局降分)、主验收盘(戊日)所在行未吸收故前面所有旧断言数值不受影响(证明分批吸收零副作用)。

---
> **以下为 M1(甲木行试点)。**

### Added 新增 · M1 · 条例层 + 受控词表 + J1 接线
- **`tiaohou.json` 升 v2:每格从「取干数组」升级为「条件树」。** 此前 skill 只知道正月甲木的头两个字「丙癸」,而书里真正值钱的是后面那串条件分支——
  「一派庚辛,主一生劳苦克子刑妻」「一派壬癸又无戊己制,名水泛木浮」「支成木局,得庚为贵,无庚必凶」。
  **丢掉的这些恰恰是确定性可判的**:丙透没透、癸藏没藏、是不是一派庚辛,全是查盘面就能判的事实。
  本批吸收 **甲木行 12 格 / 110 条条例**,每条带 `若`(受控词表条件) / `则`(典籍原判,照录) / `档`(上中下忌) / `名` / `意象` / `细化于`,
  并保留 `原文` 全文与 `病`(典籍明指之病,如 甲/子 的「癸水司权,为火金之病」)。**`取干` 一字未动,原快照锁不受影响。**
- **受控词表 v1 + 求值器 `bazi-enrich/tiaohou-tiaoli.ts`(J1)。** 谓词:`透/藏/有/无/制/支/支五行/会局/成派/十神`,计数写法 `透数:丙>=2`,组合子 `且/或/非`+括号。
  词表外的写法在 `npm test` 里直接 FAIL——这是「层1 机器可判」的守门人。求值结果进 `enrichment.调候条例`,再进 chart.txt。
  三合/三会表直接复用 `zhi-relations` 的 `SAN_HE`/`SAN_HUI`(该文件仅加 `export`,判定逻辑一字未动),不另立第二份常量。
- **三层分离落地(工单 §2.2)**:层1 盘面判定受控、层2 典籍论断照录、层3 用户解读自由。schema 给的是 `意象`(概念级素材,如「早春寒木得暖阳」),
  **不是句式模板**——严禁在提示词里为任何条例配固定白话句式(v3.7.1 正缘画像「单一句式 → 全盘同款文案」的教训)。chart.txt 在每条 `意象素材` 后原地打了这句警告。
- **`examples/sample-chart-jiamu.txt`**:甲日样例盘(合成生辰 1988-03-10 12:00 男 → 戊辰 乙卯 甲子 庚午),chart.txt 实测命中 6/18 条(上3 中1 忌2),即 M1 验收物证。
  原 `sample-chart.json`/`.txt`(戊日)同步重生成,走的是「该格尚未吸收」的优雅分支——顺带向 LLM 明示「不得凭空引用条例名」。

### Added 新增 · M1 · 测试
- `test-tiaohou.ts` 扩条例层七组断言(全绿后 261→**279** 项):
  ⓐ 结构(12 格齐全、id/名唯一、档位合法、`细化于` 指向存在、`_词表` 与求值器 `谓词表`/`成派阈值` 逐项对齐)、
  ⓑ 受控词表(每条 `若` 可解析且只用词表内谓词)、
  ⓒ **层2 照录护栏——每条 `则` 必须是本格 `原文` 的连续子串**(标点归一后)。这条最要紧:吸收 ≠ 转述,典籍论断被悄悄改写成我们自己的话,机器会拦下来、
  ⓓ 求值器行为(7 个手工盘,该命中的与**不该命中的**都断言;含 `非 透:癸` 这类守门条件的反例)、
  ⓔ 条例快照锁(与取干表同规格,静默改动立即暴露)。

### 校勘 · M1
- **影印本抽查甲木行 5 格**(工单 §2.4 要求每行 4~5 格):书页 72 / 73·76·77 / 86·87·88 / 90·91 / 126·127。一致 2 格,**新发现 3 处两系分歧,已两版并存不择一**:
  - `甲/辰`:知砚斋「或见戊己,及比劫多者,名为**杂气夺才**」 vs 影印本「或見比肩及乙多者,名為**混奪財神**」——条件字与名皆不同。
  - `甲/丑`:「庚透丁藏 / 丁透庚藏」两条的**高下在两本中互易**(知砚斋 小贵/小富贵;影印本 小富貴/富貴,并附「以庚取貴,以丁取富」为据)。
  - `甲/巳`:影印本三夏总纲「用神先丁次庚」不提癸,知砚斋逐月「四月…先癸後丁」——总纲层分歧,影印本逐月正文未通读,只登记不定案。
- 顺带校勘:`甲/寅` 知砚斋电子本文本层作「得丙癸**逢**」,影印本作「**透**」,判为电子本讹字,本表从「透」。
- 新增待核疑点 3 条(`甲/辰` 取干次序、`甲/巳` 总纲分歧、`甲/丑` 档位分歧),疑点清单 3→6 条。

### 独立复核发现并已修(M1 交付前)
条例写完后跑了一遍独立复核 + 全库穷举审计,抓出四类问题,**全部已修并各自加了永久性测试**:
1. **1 条恒真条例**:`甲申-壬无碍见戊` 写作 `有:壬 且 有:戊`——而申恒藏庚壬戊,**每张七月甲木盘都命中**,
   原文的「须见戊土,方可制水存火」一次也不会被检验。同因还有 8 处合取项形同虚设(`甲辰-杂气夺财` 的「见戊己」、
   `甲午-乏癸用丁` 的「有丁」、`甲酉-木局取庚` 的「干透比劫」…)。
   → 一律按【天干透出】判并写 `判定备注`;测试新增**恒真审计**(每格 4000 随机盘全中即 FAIL),与原有的死条例审计一头一个。
   顺带把「甲多」的口径统一为 `十神数:比劫>=2`(此前 `甲/戌` 用 `十神:比劫`,一个藏乙就算「多」)。
2. **一对相反论断会同时打出**:`甲卯-刃杀得财`[上]「英雄独压万人」与 `甲卯-癸困财杀`[忌]「主为光棍」——
   原文「柱中逢才,英雄独压万人。**若见癸水**,困了才杀」是把後句写成前句的例外,我们漏了排他守门。→ 加 `且 无:癸`。
3. **ⓒ 照录护栏有个洞**:归一化把 `／`(原文片段的拼接标记)也抹掉了,于是横跨两段不相邻原文的伪引文能蒙混过关。
   → 改为按 `／` 切段後逐段比,并把 `甲/戌` 那段用句号无标记缝起来的 6 段原文改成显式 `／`。
   同时在测试里写清这道网**防不了什么**:它只跟同一 JSON 里手写的 `原文` 比,是自洽的循环,防不住「抄的时候就抄错」——
   那靠人工对素材原书。M1 已把 110 条逐条对过 `素材/穷通宝鉴.pdf` 文本层(109/110 逐字命中,唯一例外是有影印本背书的校改)。
4. **`细化于` 被用在两种含义相反的关系上**:子集(母子同时命中,`档位计` 双计)17 例 vs 互斥(母条的例外,永不同时命中)10 例,
   而 chart.txt 对两者一律打「勿与母条重复计」——对互斥那 10 例纯属误导。另有 10 条**未声明** `细化于` 但实为真子集的,一点提示都没有。
   → 新增 `fixtures/bless-tiaoli-relations.ts`(固定种子采样算 `关系`/`蕴含` 并回写 JSON),chart.txt 按关系分别提示;
   测试用同样方法复验——**改了「若」却忘了重跑祝福脚本,会被拦住**。

### 边界(说在前面)
- **吸收 ≠ 全信**:`档`(上/中/下/忌)是书的判断不是我们的实证;chart.txt 每次输出都附一行「档位口径」,要求解读层转写为倾向性表述,不得写成宿命断语。
- **「成派」阈值是我方操作化定义,不是典籍原文**:典籍只说「一派庚辛」没给判据。现取「该五行含藏干加权分 ≥3.0 且至少一字透干」,
  命中时一并输出加权分便于审计;改阈值 = 改词表版本。此免责声明同时写在 `tiaohou.json._词表` 里(测试断言它必须在)。
- 徐乐吾评注 / 李铁笔语译层**本轮不吸收**,只吸收原文条例。

---

## [Unreleased] · v3.10.0 开发中 · 顺逆双轴 + 校准基础设施
> 本段随开发推进,发版时改为正式版本号。

### Added 新增
- **顺逆双轴(P0)**:`bazi-enrich/shunni.ts`——发用线(出口喜忌,逻辑未动)与护体线(调候命中:先1.0/次0.7/再0.5,本干透出×1.5、同五行透干×1.0、地支本气得气×0.8)分开计,`合成` 只出二维标签不给数值;方向(顺/平/逆)与振幅(静/动/剧动)正交;另加「十神事件」标注轴(方案 C,只标注不计分)。顺逆从渲染层下沉到 enrichment,chart.txt 打印方向,海报与长文同源。`fixtures/test-shunni.ts` 21 项断言。
- **调候表外置 + 校勘层**:`calculator/tiaohou.json`(120 格 + 底本状态 + 校勘块),与 `shensha.json` 同规格。此前调候硬编在 `tables.ts` 里、无出处、无校勘状态、无任何测试保护——**同类数据两套待遇**,而 v3.5 的「丁日寅月误作庚壬」正是这类事故。
- **`fixtures/test-tiaohou.ts`**:结构完整性(120 格/合法天干/无空)+ **快照锁**(内容哈希,静默改动立即暴露,有意改表须显式祝福)+ 寒暖燥湿宏观不变式(冬取火/夏取水,日主同气豁免;已知例外锁成清单)+ 行内重复度指标 + 接线一致性。**刻意不写逐格权威断言**,理由见下。
- **`references/calibration.md`**:三类校准(典籍一致性 / 现实事件 / 对照组)的分工与规程,含调候校勘提交格式、Rodden Rating 映射、随机盘对照的必要性。

### Changed 变更
- **排盘年份下限 1900 → 1800**:原限制是防御性输入校验而非算法约束(日柱/节气/农历全来自 lunar-typescript;`ganzhi.ts` 里以 1900-01-01 为锚的 `getDayGanZhi` 是死代码、无人调用)。放开的直接动机:路线图第二梯队的「名人命例回测」所用经典命例大多在 1900 年前,**此前连排盘都被拒,这条路线是被自己的输入校验堵死的**。`case-schema.json` 的 `year.minimum` 同步。
- **海报顺逆配色语义**:色阶只承载方向,不再做重级降级(降级会把刚拆开的两轴又揉回一个色阶);振幅另出 `amp_label`,模板未用则忽略。
- `case-schema.json` 加 `case_type: [典籍判定 / 现实事件 / 对照组]`——三类评分方式不同,混一个 schema 会互相污染;且典籍命例不含隐私、可入库当回归。

### Fixed 修复
- **重级降级口径不一致**:`hitWeight()` 定义重级为 天克地冲/伏吟/岁运并临/冲提纲,但渲染层两条降级路径各自硬编了类型表——大运路径漏「岁运并临」「冲提纲」,流年路径漏「冲提纲」。现统一走 `hitWeight()`。

### 已知问题(校准发现,未修)
用 1893-12-26 08:00 男(癸巳 甲子 丁酉 甲辰,时辰属传抄级)回测 15 个有确切纪年的制度性节点:**命中 5 / 反向 5 / 判平 5**。暴露四点:
1. **七杀格身弱,官杀年既升也降**,五行喜忌无区分力——格局线(杀格逆用:食神制杀/印化杀)在 `出口` 单值裁决时被扶抑线的「忌官杀财」压倒。
2. **破型重级引动不进方向**:1976 岁运并临只算振幅,方向仍判「顺」。此前的 downgrade 会压成「平」——**就这一点而言正交化改差了**。拟修:区分「破型」(天克地冲/伏吟/岁运并临,进发用线 −1)与「中性型」(冲提纲/交接,只算振幅)。
3. **调候与扶抑对冲无标记**:该盘调候取甲庚而扶抑忌金,庚金年两轴给相反信号,解读层会读成两个独立信号。
4. **振幅区分度不足**:57 个流年里「静」仅占 26%。

> 方法论限定:n=1、事后对照、且八字无被证实的预测效力。本次校准检验的是**工具内部是否自洽、有没有系统性偏置**,不是「八字准不准」;上述四点值得改是因为它们在**体系内部**就说不通,不是因为对上了史实。

## [Unreleased] · 路线图（v3.9.1 后重排）
> 路线图清账：紫微独立海报 ✅v3.7.0、流年问答 ✅v3.7.0；原第一梯队四项 ✅v3.8.0（体检器 `--mode=zonghe`/`--mode=ziwei`、随包样例口吻修正、决策门流派小白化、真太阳时 `--longitude`，详见 [3.8.0]）；晚子时约定显式披露 ✅v3.9.0（显式口径**开关**仍留此处）、命例回测**脚手架** ✅v3.9.0（数据收集与评分标准仍留此处）；`jieqi.ts` 粗近似死代码三函数 ✅v3.5 已删；**合婚双盘 → 用户定不做（2026-07-20），除非另行拍板不再列入**。

**第一梯队（精度与口径）**
- **早晚子时口径开关**：v3.5 已统一为「归次日」且有边界回归、v3.9.0 已显式披露约定；再进一步做成显式选项（今日 vs 次日两派口径可选并标注）。
- **高级开关面板**：阴干羊刃、魁罡戊辰变体、空亡阴阳轻重分级——shensha.json 已留 optional 字段，统一暴露成 config。
- **综合印证流程减重**：双盘对账骨架（大运/大限时间轴对齐、换运年重合检测、六维信号并置）下沉到算法层确定性产出，LLM 只做语言融合——省 token、防漂移（QC 评审帮助性第 2 条，工作量中大）。

**第二梯队（质量与工程）**
- **命例回测集**：收录已知名人盘/经典命例，对格局、用神、关键神煞断言做回归（防提示词改动引起解读漂移）。✅v3.9.0 已建 `fixtures/calibration/` 脚手架（schema+runner+人工评分对照表）；数据收集与评分标准仍待做。
- **金标样例库扩充**：四条海报线的随包样例 ✅v3.9.1 已补齐并进回归（`fixtures/test-golden.ts`）；仍待做——给事业/健康/合冲刑害各字段补字段级 golden example，**四型正缘画像各配一条金标**（现有样例只覆盖印星型）。
- **输出 JSON 键名中英混用**：204 个键里 114 中文 / 90 英文，`enrichment` 同层就有 `confidence_tier` 与 `五行旺相` 并排。全量改名穿透 render / check-analysis / 五份提示词，属破坏性变更，留待 4.0；当前先靠约定（机制/元数据用英文、命理概念用中文）约束新增字段。
- **低危代码清理**（QC 评审 #11/#13 遗留）：`index.ts` 身宫下标坐标系注记、虚岁公历年差口径、`shensha.json` 三奇注记/空亡 base 与实现漂移。（`jieqi.ts` 粗近似死代码三函数 ✅v3.5 已删，见顶部清账。）
- **README.en 同步**：版本徽章与样例文件名 ✅v3.9.1 已同步；正文功能描述仍停在早期版本，与 v3.9 差距较大。

**第三梯队（传播·可选）**
- **一屏分享卡模式**：页脚分享提示已做（v3.7.0）；进一步做「判词 + 小人 + 三句核心」的朋友圈比例分享卡（render 加 `--mode=card` 或各海报附带一屏卡区块）。

## [3.9.1] · 2026-07-29 · 工程卫生:发布单一事实源 + 规格收敛 + 四线样例对齐
> 本版不动排盘算法与解读口径,只修「同一件事写在多处、发版靠手记」造成的结构性隐患。
> 触发点:审计发现 GitHub 上 VERSION 停在 2.7.0 而内容已到 3.4——网页上传时漏传了 VERSION 这一个文件;
> 同一个根因还产出了「v3.8 的 .skill 里混进 33 个陈旧 dist 文件」和「仓库里躺着 _sync_probe_bash.txt」。

### Added 新增
- **`scripts/release.sh` 发布唯一入口**:`./scripts/release.sh 3.9.1` 一次改齐 VERSION / calculator/package.json / README.md 徽章 / README.en.md 徽章,重建 dist-bundle,按**白名单**打包,并在压包前校验包内无 `dist/` / `_probe*` / `*.case.json` / `node_modules`。版本号不再有第二个手改点。
- **`calculator/spec.json` 形态规格单一事实源**:判词字数(7字/4+4)、各类段落句数与字数下限、连接词白名单、timeline 项数、禁词四层分层、童年断言词表、正缘四型锚头,全部收进一份 JSON;`check-analysis.ts` 直接 import,不再在代码里散落字面量。
- **`fixtures/test-spec-sync.ts` 规格漂移哨兵**:逐条比对 spec.json 的数值与 SKILL.md / 四份提示词里的复述,对不上直接 FAIL。v3.7.1 那种「SKILL 说≥6句、提示词说3~5句、体检器按2~6拦」的三处打架,从此在测试阶段就暴露。
- **`fixtures/test-golden.ts` 金标样例回归**:四条海报线的随包 analysis 各自过对应 mode 体检,四份渲染产物不得残留 `{{占位符}}`。
- **补齐缺失的随包样例**:`examples/sample-analysis-bazi.json`(八字线此前只有渲染产物、没有 analysis)与 `sample-analysis-mbti.json` + `sample-mbti-report.html`(MBTI 线此前两样都没有);均已过体检 ALL PASS,`sample-bazi-report.html` 按新金标重新渲染以保持自洽。
- **`references/` 参考层**:`file-map.md` / `install-and-deps.md` / `troubleshooting.md` / `walkthrough.md`,按需读取。
- **`npm test`**:11 项 fixtures 一条命令串起,不再靠 TEST-GUIDE 手抄。

### Changed 变更
- **SKILL.md 重排与瘦身**(330 行 15972 字 → 267 行 12953 字,-19%):删掉 5 处内联版本号(v2.7/v1.5/v2.4/v3.1.5/v3.7——铁律 13 自己就说版本号史属幕后);`#### Step 3 — 长文版/海报版` 改 `3a/3b`,不再三层重复 "Step 3";**八字海报的 `render --mode=bazi` 命令归位**(原先被隔在紫微、MBTI 两节之后,顺读会误以为属于 MBTI),并入三段流水线成为第 4 步;三条例外(放弃选择/小白模式/流年直达)统一收在 Step 0 顶部,门禁段不再重复;文件清单/工作示例/失败模式表移入 `references/`,正文只留一张「需要时再读」指引表。
- **`examples/` 四线命名对齐**:`sample-analysis.json` → `sample-analysis-zonghe.json`、`sample-report.html` → `sample-zonghe-report.html`(此前只有综合线没有线别后缀);`test-check.ts` 与 `README.en.md` 引用同步。
- **`package.json`**:`version` 由 `0.1.0` 改为跟随 VERSION;`chart`/`dump`/`render` 三个 script 由指向 `dist/` 改为指向 `dist-bundle/`(消除「跑到陈旧编译产物」的入口)。
- **TEST-GUIDE**:补 `npm test` 一键跑法与新增两项 fixtures,并注明 fixtures 必须用 `npx tsx` 从 `calculator/` 跑源码。

### Fixed 修复
- **`calculator/dist/` 退出版本库**:它曾入库并随包分发,且停在 v3.8——缺 `confidence.js`/`liuyue.js`/`shichen-boundary.js` 三个 v3.9 模块,而 SKILL.md 与 package.json 都把 `node dist/run-chart.js` 写成可选路径,一旦走这条会**静默跑出旧逻辑且不报错**。现加入 .gitignore、不入库、不随包;误入库的 `dist/_probe.txt` 一并删除。
- **`.gitignore` 白名单误伤**:`*-report.html` 规则实际命中 `examples/sample-bazi-report.html` 与 `sample-ziwei-report.html`(`git check-ignore` 实测),而白名单只放行了三个具体文件名;改为整目录放行 `!examples/` + `!examples/**`,新增样例不会再被静默吞掉。
- **两个 README 版本徽章停在 3.8.0**:v3.9.0 的收尾提交动了 7 个文件却漏掉它们;现由 release.sh 统一改写。
- **MBTI 判词阈值与提示文本不一致**:`check-analysis` 按 34 字判定却在报错里写「>30字」,统一从 spec.json 取值。

### 未做(有数据支撑的主动放弃)
- **不合并 `render.ts` 的 `chartToFlat` / `chartToFlatBazi`**:实测两者 186 / 178 有效行里逐行完全相同的只有 11 行(6%),最长连续相同块 3 行,且全是 `return out;` `} else {` 一类样板。它们服务两套不同的模板变量集,是**平行而非重复**,强行合并只会制造耦合。
- **不抽取模板公共 CSS**:zonghe↔ziwei 逐字相同的规则有 37 条(3506 字节,占 ziwei 模板 18.7%),但 bazi↔zonghe 只有 10 条、mbti 与谁都是 0 条。且 `render.ts` 是纯占位符替换、没有 include 机制,为这点重复引入模板包含功能不划算,留待有第五条海报线时一并考虑。
- **不统一输出 JSON 的中英文键名**:204 个键里 114 中文 / 90 英文(`enrichment` 同层就有 `confidence_tier` 与 `五行旺相` 并排)。全量改名会穿透 render / check-analysis / 五份提示词,属破坏性变更,不进 patch 版。

## [3.9.0] · 2026-07-28 · 准确性三连(时辰边界/晚子时/置信度传播) + 小白模式 + 流月与多年对比
> 主线:全链准确性此前只防了「LLM 排盘错」,没防「输入错」——本版把时辰输入可信度纳入管辖;并新增触发式小白模式(深度模式一行未降级)。

### Added 新增
- **时辰边界检测与核盘流程(P0-A)**:`bazi.enrichment.时辰边界`——出生时刻距最近时辰交界(奇数整点)|Δ|≤20 分钟 → `boundary:true` + 相邻候选时辰;未传 `--longitude` 附 solar_note(钟表时间口径,中国境内偏差约 -3h~+30min)。dump-text 顶部渲染「⚠时辰临界」施工图块;SKILL 新增 Step 1.5 **阻断性核盘分支**(追问出生地重排 / 双盘核对过往大事年份 / 用户坚持则降档继续)。`fixtures/test-shichen.ts` 7 组边界回归。
- **晚子时约定显式披露(P0-B)**:实测确认引擎为【换日】约定(23:30 盘 ≡ 次日 00:30 盘)后,出生 ∈ [23:00,24:00) 时输出 `bazi.zishi_convention`,dump-text 渲染提示行;bazi/ziwei 提示词加白话披露强制规则;SKILL 失败模式表补条目。
- **全局置信度传播(P0-C)**:`bazi.enrichment.confidence_tier`(high/medium/low,聚合规则=时辰临界/用神边界盘/三线收敛,写成 confidence.ts 头注可复现);bazi-prompt 新增「置信度传播(强制)」——low 档全部预测性章节条件句/区间应期/禁高确定措辞/锚点声明一次;`check-analysis --mode=longform` 新增 `_边界盘高确定断语` 红线(HIGH_CERTAINTY_WORDS 常量 + 无条件词单年断事启发式)。
- **小白模式(P0-D,触发式旁路)**:`prompts/novice-mode.md`——新手信号识别(识别不到零打扰)、决策门三项坍缩为一问(流派全程隐形)、双层锚点(人话版 3~4 句先出/技术版收起)、章节菜单重排(事业/财运/婚恋/健康/未来五年在前,五行十神神煞并为「背后的推理过程」)、速览末尾核对点(严格禀赋句;时辰修正显式重跑接 P0-A,其余反馈不得无声改锚点)。SKILL description 补小白触发描述;深度模式一行未降级。
- **流月粒度(P1-A)**:`--currentYear` 时输出 `bazi.enrichment.流月引动`——12 节气月(精确节气时刻分月,五虎遁起月干)逐月 vs 原局/该年大运引动,**复用**运岁检测器(suiVsYun 加 label 参数,缺省行为不变);liunian-qa 支持「下半年/几月适合…」月级窗口(农历月序+公历对照)。`fixtures/test-liuyue.ts`。
- **多年对比(P1-B)**:`--compareYears=Y1,Y2,…`(≤5)输出 `bazi.enrichment.多年对比`(逐年引动+用神出口喜忌评分+重级标记);liunian-qa 新增比较型问法(单一领域逐年顺逆梯度+相对排序,给信号不给指令)。`fixtures/test-compare.ts`。
- **校准回测脚手架(P2,只搭架子)**:`fixtures/calibration/`——命例 schema(生辰+时辰来源可靠度+已证实事件年表+四模块评分维度)、runner 骨架(排盘→待人工评分对照表)、职业映射空 schema 占位;`*.case.json` 入 .gitignore。

### Changed 变更
- **大运边界表述统一(P1-C)**:全部面向用户输出一律「2008–2018 年(约 8–18 岁)」——年份为主(算法层 startYear/endYear)、年龄为辅冠「约」,规避虚岁/周岁歧义。render 三处注入(八字海报大运行/综合海报大运条与阶段印证八字行/当前大运标签)、yunsui 大运引动.年龄字段、dump-text 大运块、五份提示词措辞规则逐一落实;紫微大限保持虚岁口径(不在本项范围)。
- 随包样例 `sample-chart.{json,txt}` 按新引擎重生成(含时辰边界/confidence_tier/年份为主大运行)。

## [3.8.0] · 2026-07-20 · 路线图第一梯队清账:门禁对齐 + 样例修正 + 真太阳时
### Added 新增
- **体检器覆盖补全**:`check-analysis` 新增 `--mode=zonghe`(综合海报此前零脚本校验,QC 评审点名)与 `--mode=ziwei`——判词 7字/4+4、全局禁词/机制泄漏/频率断言/童年断言、播报腔(主叙事字段禁「此命/该命/命主」)、段落长度、枚举与条目数;三条海报线门禁对齐,SKILL 流程接入"渲染前先体检"。test-check +9 断言。
- **真太阳时可选校正**:`run-chart --longitude=<东经>`(默认关,现行为不变)。口径:真太阳时 = 东八区钟表时 + 经度差((lon−120)×4min) + 均时差(NOAA 通行近似式,误差<1min);复用 v3.5 跨日归一化管线,可能改动时柱/日柱。test-boundary +4 断言(120°E 六月≈零校正/90°E −120min/二月均时差 −14min/缺省完全不动);SKILL 出生地改为"可选、不主动追问"。
### Fixed 修复
- **随包样例二次修正**:`sample-analysis.json` 判词「厚土紫微的守成者」为 8 字违反 7字/4+4 规格 → 「厚土紫垣守成客」;主叙事(section_01/02)由第三人称播报腔改写为第二人称(评审点名的隐性坏 few-shot,此前只修了机制泄漏未修口吻);改后过新 `--mode=zonghe` 体检 ALL PASS(样例即 golden 进回归)。
### Changed 变更
- **决策门流派小白化**(QC 评审帮助性第 3 条):五个流派选项加人话标签与推荐位(子平——最主流·看格局层次·新手推荐 等),只改文案不改机制。

## [3.7.1] · 2026-07-20 · 约束体系审计:松掉制造同质化的三个点位(护栏与复现约束不动)
> 用户质询「limit 是否过紧导致同质化/质量差」触发的全量约束审计。结论:护栏类/复现类/版式类/下限类约束定位正确保留;3 个「风格级硬模板」在批量生产同款文案、1 处口径矛盾在压质量上限——本版修正。
### Fixed 修复
- **精读段口径矛盾**:SKILL 说「段落≥6~10句不限篇幅」、bazi-poster 说「精读段3~5句」、体检器按 2~6 硬拦——模型写得越满越被打回。统一为:四大解读段≥6句不限上限、精读段 3~7 句(体检器容差 2~8),SKILL/提示词/评审遍/体检器四处对齐。
### Changed 变更(反同质化)
- **正缘画像四型分型**(原评审遗留项):废除全盘一刀切的单一句式,按「正缘倾向.宫坐」确定性分型选锚头——印星→『你适合的另一半…』/官杀→『能接住你的…』/财星→『让你眼睛一亮又留得住的…』/食伤比劫→『与你最同频的…』。分型由算法宫坐决定,同盘可复现;体检器新增锚头×宫坐一致性校验(错型必拦),年龄照抄/骑墙/标绿规则不变。长文与两海报同步。
- **两句块连接词白名单**:第二句开头由强制「所以你」×4(一张海报连读四遍,节拍器式同构)放宽为六选一白名单(所以你/意味着你/这让你/落到你身上/这股劲让你/放到生活里),并要求同海报各块错开;体检器同步。
- 「恰两句」「判词7字/4+4」「timeline恰5项白名单」「标签字数上限」等版式/复现/品牌约束**有意保留不动**。
### Added 新增
- test-check 回归 +6 断言:分型错位必拦/正确分型零误伤/新连接词通过/白名单外被拦/精读段7句通过/9句仍拦。

## [3.7.0] · 2026-07-20 · 体验与新功能(QC 评审第三刀,合婚除外全落地)
### Added 新增
- **紫微独立海报(2+B/2+C)**:`templates/report-ziwei-poster.html` + `prompts/ziwei-poster.md` + `render --mode=ziwei`。十二宫盘为视觉核心(与综合海报同源结构:命宫朱框/身宫徽标/当前大限高亮/生年四化四色),盘面全算法注入、LLM 只产解读 JSON(9 版块:命格主轴/命身三方四正/事业/财帛/婚恋/健康/大限/重点流年三档/建议);判词与八字海报同规格(7字/4+4)。三条产品线海报形态补齐。
- **流年问答模式(直达通道)**:`prompts/liunian-qa.md`。用户直接问「我 2027 年适合跳槽吗」类问题时跳过决策门,以 `--currentYear=<问的年>` 重跑排盘取该年运岁引动+喜忌+作用裁决作答,轻量单问单答、答完即停;守顺逆措辞/护栏/不替用户做决策。
- **古风小人接入八字海报**:MBTI 海报的 32 变体小人(最像类型×性别)进入八字海报头部,三线视觉统一;八维结构缺失时槽位自动隐藏。紫微海报头部同样接入。
### Changed 变更
- **字体国内可达**:海报模板字体源由 fonts.googleapis.com 换为国内可达镜像(fonts.loli.net,API 兼容),并全模板加强系统中文衬线回退栈(Source Han/Noto CJK/Songti/STSong/SimSun)——镜像失败也不破版式。
- **分享最后一公里**:三份(现四份)海报页脚统一加「📱 长按或截图保存,即可分享」提示。
- SKILL.md:Step 0 紫微线增加海报形态问答与加载表(2+A/B/C);新增流年问答直达通道说明;文件清单同步。

## [3.6.0] · 2026-07-20 · 质量硬化(QC 评审第二刀):三线护栏 + 长文体检 + 机制注清理
### Added 新增
- **长文后置体检**:`check-analysis` 新增 `--mode=longform`,对八字/紫微/综合**长文**做机器可判的形态红线扫描——幕后机制泄漏 / 版本号史 / 第三人称播报腔 / 绝对凶语(顺逆措辞) / 行为频率断言 / 童年行为断言 / 正缘年龄与算法判定一致性。泄漏样必拦、合规样零误伤(「置信度」「命主星」「顺风/逆风」等合法词不误杀),已进 `test-check` 回归;SKILL.md Step3 长文版接入自检步骤(三线通用)。
### Changed 变更
- **成长心态 disclaimer 三线统一**:此前仅八字线前置;紫微线、综合印证线现同样在输出**最开头**整段前置 `disclaimer-preamble`(两段全文),三线心理护栏等级一致。`disclaimer-preamble` 措辞由「八字」通用化为「命盘/命理」以复用三线;SKILL.md 加载表(选 2 / 3+A)与关键约束 13 同步。
- **海报判词规格对齐**:综合印证海报判词由旧「3–7 字」升为与八字海报**同规格**「7 字 或 4+4 对仗」。
- **提示词保守瘦身**:`bazi-prompt` / `bazi-poster` 里重复两遍的「可证伪事实护栏 / 现实断言边界」合并为一块(三组示例与禀赋/回报/条件句、锐度不降、句式原则全保留,零机制丢失);删去规则标题里面向用户可见的版本号标签(v1.5…v3.2.3,规则内容一字不改),仅保留幕后 JSON schema 注释键中的版本号。行数 bazi-prompt 173→166、bazi-poster 151→144。
### Fixed 修复
- **幕后台前分离·模板机制注清理**:MBTI 海报 01 版块标题去「(由四柱十神强度确定性折算)」、八字海报 05 版块标题去「(状态由算法层裁决)」——面向用户文字不再泄漏算法层。
- `bazi-prompt` 运行前装配编号修正:调候基准由半号 `4.5` 顺排为 `5`,`open` 默认派顺排为 `6`。

## [3.5.0] · 2026-07-20 · 边界接线层批量修复(QC 评审驱动)
> 一次系统性质检发现:核心排盘规则抽查基本全对,但「边界接线层」积了一批准确性 bug,其中两个会导致整张盘排错。本版逐条修复并新增 `fixtures/test-boundary.ts` 边界回归护栏。
### Fixed 修复(P0·会致整盘错)
- **紫微「阴阳男女」误用公历年干**:原按公历年干判阴阳、安星却用农历年干,自相矛盾——**公历元旦～立春前出生者 100% 触发**,阴阳判反导致大限方向整体走反。改统一取农历年干。(随包样例 `sample-chart` 曾把该错固化为「阳男」,本版一并重生成为正确的「阴男」。)
- **`--isLunar` 农历输入被静默忽略**:原一律按公历解析,用户按文档传农历得到无警告的错盘。农历输入(含闰月,负月份=闰月)现真正生效。
- **`--timeZone` 时区被静默忽略**:非东八区出生者时柱/日柱/紫微生时全错。现接入换算(支持半时区),端到端可复现。
### Fixed 修复(P1·局部规则)
- **调候表丁日寅月**:`庚壬` → `甲庚`(《穷通宝鉴》:非庚不能劈甲、非甲不能引丁)。
- **午月误作纯气**:午藏丁己二干非纯气;甲日午月己土透干现正确取透立正财格,不再恒立伤官格。
- **紫微晚子时与八字不同步**:23 点后八字日柱推次日、紫微仍用当日——现共用同一 effective lunar,两侧口径一致。
- **旺衰墓库误判**:墓库有根不再同绝胎养判 −3;极旺阈值抬高并加约束,普通偏旺盘不再被误引向从格。
- **岁运漏检**:伏吟不再吞掉同支自刑;流年×大运补查相刑/自刑。
- **拱会滥报**:仅夹拱中位者算拱会,其余三会两支作方气半会。
- **死代码修复**:`ganzhi.ts` 干支基准日注释与实现由误作「己丑日」更正为「甲戌日」。
### Added 新增
- `fixtures/test-boundary.ts`:阴阳年干口径 / 农历闰月输入 / 时区换算 / 晚子时同步 / 调候 / 格局纯气 / 岁运刑冲 / 拱会 / 旺衰 九类边界回归,覆盖上述最危险的「元旦～立春前」区间。
### Changed 变更
- 移除 `yiqi-core/bazi.ts` 遗留的调试打印(`🔍` zhangSheng/naYin、`[晚子时处理]`),此前每算一盘都会污染 stderr。
- `examples/sample-chart.{txt,json}` 按修复后引擎重生成;`sample-analysis.json` 置信度备注去除「算法模型」机制泄漏(遵 v3.1.5 幕后台前分离)。

## [3.4.1] · 2026-07-15 · R6 忌神改取扶抑线(治「格局喜水掩盖满盘水克身」)
### Fixed 修复(用户定)
- 八维 R6 忌神折向的忌神来源由「出口.忌神」改为**扶抑线原始忌神**:出口忌神经过开运/喜神冲突过滤,格局喜水会把水从忌神剥掉——但满盘皆水时水就是克身之忌(大水冲了龙王庙),八维看的是承压事实、不看格局取用。附带修正:中和临界盘扶抑忌为空时不再折向(中和谈不上克身)。10 盘抽测 7 盘存在过滤分歧,均已按扶抑口径计算。

## [3.4.0] · 2026-07-15 · MBTI 海报:意象嫁接 + 叙事主轴翻转
### Changed 变更(用户定)
- **意象嫁接**:日主意象(十干定表)成为 MBTI 海报公共母题——mbti_tagline/diff_verdict/diff_html 各须出现一次「日主意象×MBTI特质」融合画面(金标为假想例:INTJ底盘×ESFP实测×壬水=「你日柱那条沉静的江河,翻起了浪花」),七版块鼓励呼应;串盘意象为重大错误。
- **叙事主次翻转**:七大版块以八维功能语言为主轴("你的 Te 主导让…"),八字证据压缩为一句落锚,不得再以八字术语开头、MBTI 点缀。
- 体检器新增三条机器校验:tagline 未落日主意象 / diff 判词与正文均无日主意象 / overview 未点名主导功能,违规样必拦、合规样零误伤。

## [3.3.2] · 2026-07-15 · 逍遥巾飘带修复
### Fixed 修复(用户反馈)
- N-P 男版逍遥巾飘带原为两截式,尾端三角悬空似污点+左束带线头飘出发髻——改单条连体飘带,束带锚进发髻;32 变体校验加"无悬空残片"断言。

## [3.3.1] · 2026-07-15 · 女版小人双嘴修复
### Fixed 修复(用户反馈)
- 女版小人叠了男式笑线+红唇两张嘴——改为性别分流:女版红唇直接替换笑线(E 微扬/I 含蓄),32 变体校验升级为"每变体恰一张嘴"+唯一性+严格 XML,全过。

## [3.3.0] · 2026-07-15 · rubric v4(忌神折向/格局复合/身弱E轴/华盖加重) + 小人分男女
### Added 新增(用户定,五项)
- **R6 忌神折向**:官杀为忌=被克身而非掌权,能量导向承压内化——正官(忌)→Fi/Ni,七杀(忌)→Ni/Fi;忌神五行由用神算法层出口传入,仅官杀折向(食伤财印象义双向,注禁类推)。
- **R5 格局复合修正**:压制/引导关系独立计分,不等于参与十神各算一遍——伤官佩印(Ni+0.20,Fi+0.10)/杀印相生(Ni+0.20)/食神制杀(Ti+0.20);仅限透干确定性检测,注禁扩至藏干推演。
- **R7 身弱E轴负修正**:身弱=整体被外部推着走,Te/Fe/Se/Ne 终值打折——偏弱×0.88/极弱×0.80。
- **R4 华盖加重**:内向直觉标志星,v4 专用曲线 1柱+0.30/2柱+0.40/3柱+0.50/4柱+0.60。
- **小人分男女**:16 型×男女=32 变体,女版换发式头饰(高髻步摇/单髻玉簪/双环髻/束发红绳)+鬓发耳坠花钿唇色+裙摆,身体几何不动防叠压;32 变体全部通过严格 XML 解析+唯一性校验。
### Changed 变更
- `--rubric` 默认 v4,v2/v3 开关保留且逐位回退不动(fixture 断言 v3 华盖仍+0.15、v3 忽略 jiShen);v4 类型与 v3 不一致时照例降档+注明。

## [3.2.3] · 2026-07-15 · 童年断言细则(治「从小就习惯牵头攒局」类泄漏)
### Fixed 修复(用户反馈)
- 「从小/小时候/学生时代」后**只能接气质,不能接行为**:"从小带着不服输的劲"可以,"从小就习惯在同龄人里牵头,攒局、定方向、分工派活"禁止(每个动作都可被当场证伪)。规则写入三份解读提示词(挂现实断言边界之下,含泄漏原句作反例);体检器两模式加同句正则拦截(童年限定词×动作词:牵头/攒局/张罗/带头/组织/分工/派活/主持/带队/习惯/当过/干过等),泄漏原句实测必拦、气质句零误伤。

## [3.2.2] · 2026-07-15 · 验收盘去隐私
### Changed 变更
- rubric 验收盘由真实授权盘替换为**假想盘**(甲子 丙寅 戊申 癸丑,历法自洽:甲年正月丙寅合五虎遁、戊日癸丑时合五鼠遁,胎元丁巳/命宫丙寅按公式推得),v2 快照与全部断言随之重算,仓库不再含任何真实出生数据。

## [3.2.1] · 2026-07-15 · 四柱全见抬顶
### Changed 变更(用户定)
- R2 单维封顶联动计数制:默认 0.60;若任一 R2 神煞四柱全见(×4),该盘封顶提至 **0.80**——四柱同神煞是罕象,不应被常规封顶吃掉。验收盘德秀×4 由封顶实得 0.30 变为全额 0.40。fixture 双向断言:有×4 抬顶生效、无×4 仍 0.60。

## [3.2.0] · 2026-07-15 · 八维神煞计数制 + R4 性情类神煞
### Changed 变更(用户定)
- R1/R2 神煞加分改**计数制**:同一神煞按命中柱数给总分——1柱+0.15 / 2柱+0.20 / 3柱+0.30 / 4柱+0.40。驿马原"逐柱定额(日柱0.30)"并入计数曲线;德秀满盘罕象额外+0.15 取消(4柱德秀经计数制已达+0.40,再加即双计)。R2 单维 0.60 封顶保留,与计数制联动(验收盘德秀×4 名义 0.40、封顶后实得 0.30,已入 fixture)。
### Added 新增
- **R4 性情类神煞**(计数制同上,单维):华盖→Ni(孤高玄思)、将星→Te(统御掌局)、桃花(咸池)→Fe(人际魅力)。明确排除并注禁改:羊刃/帝旺(比劫本气已入 Te/Se,再计即双计)、天乙贵人(际遇象非性情象)、孤辰寡宿(六亲境遇象)、MODERN 层全部(婚恋应期类与八维无涉)。
- fixture 新增 19b/19c:计数制×封顶联动、R4 三项加分、排除项零泄漏,全部逐位断言。

## [3.1.5] · 2026-07-03 · 幕后台前分离(治机制泄漏)
### Fixed 修复(用户反馈)
- LLM 会向用户解释 rubric 变化/算法机制。新增全局铁律「幕后台前分离」:chart.txt 机制信息(rubric/审计/协议/侧重/置信推导)仅供推理,禁止向用户展示——写入 SKILL 关键约束+三份海报/长文提示词;chart.txt 算法补层标题自带幕后声明;体检器加机制词黑名单硬拦截(两模式)。

## [3.1.4] · 2026-07-03 · 能量条渲染修复
### Fixed 修复
- 八维能量条填充不显示:fill 为行内 span,width 百分比不生效 → 加 display:block。

## [3.1.3] · 2026-07-03 · 人物躯干几何修复
### Fixed 修复(用户截图反馈)
- 三处配饰几何互相叠压致躯干渲染错乱:①E 型披帛原为横穿胸口的水平色带、压住交领 → 改双竖飘带沿身侧垂落且垫于袖层之下;②SJ 笏板过大悬浮胸前 → 缩小下移至拱手位;③T 剑柄锚在腰带下方裙裾 → 上移收进腰带侧(护手+柄+首)。图层顺序重排(披帛→交领→袖→腰带→腰佩→道具)。16 型唯一性复验 16/16。

## [3.1.2] · 2026-07-03 · 回退正面站姿 + 16 型服装全差异
### Changed 调整(用户定)
- 人物回退 v3.1.0 正面低多边形站姿构图;服装差异从四类群扩到 **16 型各不相同**:袍系×道具=四类群 | 袍色明暗+披帛=E/I | 头饰=SJ乌纱/N系J进贤方冠/N系P逍遥巾飘带/SP背斗笠 | 腰佩=T剑柄/F玉环流苏。16 型 SVG 唯一性实测 16/16。

## [3.1.1] · 2026-07-03 · 劫财归维修订 + 侧身画风 + 双备选
### Changed 调整(用户定)
- **劫财映射修订**:['Se','Te']→['Te','Se']——劫财主竞争性资源调度与执行推进(Te),行动冲劲为次象。属基础分调整,v2/v3 同步生效,验收盘 v2 基线快照随之重立(Se 3.82→2.73,Te 1.54→2.64)。
- **MBTI 人物改 3/4 侧身半身像**(对齐 16personalities 参考):面朝左、近臂举道具、鼻楔+近大远小双眼+眉,折面发量/乌纱单侧长翅,四类群装扮与 E/I 神态保留。
- **标题双备选**:八维引擎新增「备选2」(去重≠最像/备选1,兜底第三辅助);MBTI 海报标题旁加两枚小备选徽章。

## [3.1.0] · 2026-07-03 · 八维 rubric v3 + low-poly 画风
### Changed 调整
- **八维评分 rubric v3**(用户 spec):R1 驿马→Ne(+0.20/处,日柱0.30;取求变求新之象,禁计 Se 防双计);R2 文气修正(文昌/学堂词馆/德秀各 Ne、Ni+0.15,德秀满盘再+0.15,单维封顶0.60,待核不计);R3 胎元×0.25/命宫×0.30 低权重虚柱(现行映射照用)。归一化重算、类型重推;v2/v3 类型不一致自动降一档置信并注明来源;「依据」逐笔审计 R1-R3;`--rubric=v2|v3`(默认 v3,v2 与旧版逐位一致,已快照断言)。验收盘 Ne 0.82→1.69,七笔加分来源与 spec 完全吻合。
- **MBTI 人物改 low-poly 折面画风**(16personalities 同款):三阶色折面袍身/发髻/道具,四类群装扮与 E/I 神态保留。

## [3.0.0] · 2026-07-03 · 八字 MBTI(荣格八维体系)
### Added 新增
- **八维引擎**(`bazi-enrich/bawei.ts`):十神→荣格八维映射矩阵(主0.7/次0.3)×十神强度(透干2/月令本气1.5/藏干按位),日主阴阳微调内外向;按功能栈规则(主辅内外相反、判断感知相反)推**最像类型+备选+置信**,确定性可复现,24 盘实测 10 种类型无坍缩。铁律:映射参考非测评,措辞「最像」禁「你是X型」。
- **八字海报第 10 版块「你最像的是」**:类型大字+备选/置信/主导辅助+八维前四条,全算法注入零漂移。
- **独立八字 MBTI 海报**(`--mode=mbti`,Step0 形态 D):头部**古风 MBTI 人物**(16personalities 扁平画风×汉服拱手礼,按四类群换装换道具——NT 紫袍羽扇军师/NF 青袍书卷文士/SJ 藏蓝乌纱朝官/SP 赭衣酒葫芦游侠,E 睁眼 I 闭目,render 按类型生成 inline SVG);八维能量条形图;**九个中式线条小图标**(太极/罗盘/双茶盏/同心结/算盘/屋檐/古琴/阴阳鱼等);性格总述/三观/朋友/恋爱观/工作/家人/爱好七段 MBTI 年轻叙事(必须落回盘面,继承全部金标/护栏规则)。
- **「实测×底盘」差异版块**:生成前必问一次实测 MBTI(不填自动留白不追问);提供则出——大写「实测 × 底盘」hero 双色大字+判词行(`diff_verdict`,句式『你是{四字概括}，{修饰语}的{身份意象}』)+约500字正文(只讲积极面与组合优势;实测=盘面则讲「表里如一」)。
- **差异版块防臆断规则(用户定·专业性与准确性为最高法则)**:①不得断言用户必定经历过什么;②**不得列举具体场景清单去猜**(列举即猜测,猜不中即失准)——只描述环境的性质一句带过(「鼓励表达、奖励尝试、给你自由度的环境,正是唤醒 Ne 的土壤」),宁可少说不可说错。通用「不列举猜测」条款同步入长文与八字海报的现实断言边界规则。
- **「能力而非事迹」全文级原则(用户定)**:解读写能力/特质/潜力,不写行为履历——「拍板兜底多半是你」类频率断言禁用,改「你有把散局重新拢起来的能力」;环境成因**两步走**:先讲性质,可补「该类别最典型样子」的原型案例(描绘类别而非用户)。体检器加频率断言黑名单硬拦截。
- **体检器 `--mode=mbti`**:「你是X型」断言/盘外类型字母/段落句数/着色/判词格式/差异版块字数(400-650)全查。官方样板海报随包验证(首稿曾被自家体检器打回,防线自证)。

## [2.7.0] · 2026-07-03 · 启动自动更新(会话级影子更新)
### Added 新增
- **self-update 脚本**(`calculator/self-update.ts`,dist-bundle 零依赖):启动时 GET 仓库 main 分支 `VERSION` 与本地比对(semver);有新版且 `--fetch=true` → 下载仓库 zip 解压到工作目录并做最低完整性校验(dist-bundle+SKILL.md),本次会话切换新版;**全程 fail-soft**——无网/404/超时/解压失败一律单行 skip、exit 0,绝不阻塞正常使用、绝不重试。
- 仓库根新增 `VERSION` 文件(机器可读版本源);SKILL.md 增「启动版本检查」节(决策门前静默执行一次,更新成功才向用户提一句)。
- **注意**:需将 VERSION 文件推送到 GitHub main 分支后检查才会生效;安装目录只读,故"重装"的实现为会话级影子更新+提示用户重装 .skill 永久生效。

## [2.6.0] · 2026-07-03 · 正缘倾向进算法层(治画像年龄跳变)
### Fixed 修复
- **正缘画像年龄同盘打架**:年长/年轻/同龄此前由 LLM 每次自由裁量。新增 `bazi-enrich/zhengyuan.ts` 通行断法确定性判定——夫妻星(男财/女官杀)位置计分(年+2/月+1/时-2,透干权重2藏干本气1)+配偶宫坐星修正(印+1/食伤-1/比劫同龄),≥2 年长、≤-2 年轻、余同龄;星不显则置信降低。chart.txt 输出「正缘倾向」行(含宫坐十神供性格特质锚定);提示词强制照抄(置信低可改性格轴);**体检器新增矛盾拦截**(画像年龄词与判定不符/置信高却未用年龄词 → FAIL)。
- 实测:同盘两次判定逐字一致;12 随机盘分布健康(年长/年轻/同龄均有,无坍缩);矛盾样本被正确拦截。回归 +3。

## [2.5.1] · 2026-07-03 · 六盘矩阵 QA(从格分歧显式化)
### Fixed 修复
- **极旺/极弱盘从格分歧未点破**:六盘矩阵实测,极弱盘(score=-8)扶抑线按普通身弱写"宜印比生扶",与格局线顺势取用的矛盾只靠分歧注隐性呈现。现极旺/极弱盘在扶抑依据显式标注「⚖若作从格论则反取顺势…重大分歧,解读须并陈」并强制边界盘;顺手修掉该标注里的模板插值失效。
### QA 备注
- 六盘覆盖:立春前+丑时/闰日(1972-2-29)/晚子时跨年(12-31 23时)/盲派高分盘/少儿盘/别名派+早子时。自动扫描(残留占位/undefined/NaN/HTML闭合/喜忌交集/胎元命宫合法性/建议节点排序)零异常;胎元命宫抽检手算一致;晚子时按23点换日口径一致。

## [2.5.0] · 2026-07-03 · 罕象检测(神煞/合冲章优先讲解)
### Added 新增
- **罕象检测器**(`bazi-enrich/rare.ts`,确定性):四库全/四生全/四正全/四支一字/天干一字/三朋/天干双合(极罕~少见)、原局天克地冲/伏吟、三合三会全、双冲对峙,及神煞叠加类——德秀满盘(≥3柱·极罕)、三德会聚(天乙+天德+月德)、三奇真格、吉星聚柱(单柱≥4吉)。各带罕见度标签与传统论法一句,什么算罕见由算法定义,不交 LLM 现场判断。
- **chart.txt 新增「罕象⭐」块**(置于神煞块前,按罕见度降序);长文神煞章与合冲刑害章、海报两个精读段**开头优先讲解罕象**(点明罕见性,语气克制不神化,仍守神煞铁律)。
- **体检器联动**:盘有罕象而神煞/合冲精读段只字未提 → FAIL 送回评审遍。
- 回归 +5:四库全极罕/德秀满盘/三德会聚/排序/普通盘无误报。

## [2.4.0] · 2026-07-03 · 评审—重生 Pass(海报质量地板)
### Added 新增
- **评审—重生遍**(`prompts/bazi-poster-review.md`,用户 spec 收编+v2.3 适配):接在首遍 JSON 后、render 前——5 通用轴(锚定/落地/口吻/护栏/规格)+逐字段类型红线评审,**只重写 FAIL 字段、PASS 逐字保留**(抬地板不抬天花板);重生必回 chart.txt 重新锚定;含护栏句式转换表与 fail→fix 样例。算法注入字段(用神四格/开运五格/配色)不在评审范围。
- **确定性体检脚本**(`calculator/check-analysis.ts`,dist-bundle 零安装可跑):把机器可判的红线从 LLM 评审下沉——JSON 合法性、判词格式(7字/4+4·禁格局术语)、全局禁词(tier/needs_review/命主/大凶/灾年)、日主固定句式、两句类计数+「所以你」开头、四大段句数字数着色下限、正缘画像句式+骑墙检测、精读段句数、yunsui 年份窗口、timeline 恰5项+建议节点白名单。FAIL 即 exit 1,送回评审遍重生(最多一轮)。
- **管线接线**(SKILL.md):首遍出稿 → 评审重生 → 脚本体检 → ALL PASS 才渲染。
- 回归:fixtures/test-check.ts(合格样本全过/判词术语/禁词/画像骑墙/白名单越界五组断言)。
### 备注
- 温度分离(首遍略高/评审低)在 skill 内不可控,以「评审遍求保守一致」指令近似;婚恋画像年龄规则按本 spec 放宽为「择一或明确改用性格轴」。

## [2.3.1] · 2026-07-03 · 四连渲染实测修复(喜忌同现/缺补说明/开运回退)
### Fixed 修复(随机缺金盘四连渲染 QA)
- **喜忌同现**:临界盘忌神回退比劫时未与喜神/开运做冲突过滤,实测同一元素同时出现在喜、忌两栏。现强制过滤;滤空时海报显示「无明显忌神(临界盘,以流通为要)」。
- **开运用神回退越界**:无共识时回退取调候首元素,可能选中扶抑明令之忌(身弱盘回退到克身之五行)。现从调候→格局候选池优先选不与扶抑忌冲突者。
- **「缺X为何不补X」无解释**:新增确定性〔缺补说明〕——五行为0的元素自动判定与用忌关系并给结论(所需→补最对症/为忌→缺反成清/无关→缺啥补啥不适用),注入用神区、写入 chart.txt,提示词强制转述。
### QA 结论
- 同盘四连渲染(四份互相矛盾的 analysis):用神区/开运算法五格/顺逆配色/胎元命宫**逐字一致**;判词与宜业宜往等创作字段按设计允许变化;timeline 年份受建议节点白名单约束。

## [2.3.0] · 2026-07-03 · 海报一致性全钉死(单值出口/开运五格/配色/时间轴) + 顺逆措辞
### Fixed 修复(同盘两版海报对比诊断)
- **用神单值出口跳变**:v2.2 稳住了"取哪条线",但开运用神/喜神格等单值字段仍由 LLM 在候选集合内自选(实测同盘一次给金、一次给木)。现 `yongshen.ts` 增「出口」单值裁决(开运用神=共识优先/喜神=发用线/忌神=扶抑忌/调候提示=原文),**render 直接注入用忌喜调候格、divergence 注、开运用神/吉方/吉色/吉数/调候五格,LLM 产出一律忽略**——两份互相矛盾的 analysis 渲染结果逐字一致(已实测)。
- **大运/流年配色波动**:顺逆配色由算法按「干支五行×出口喜忌打分+天克地冲/伏吟/岁运并临降档」确定性生成,analysis 的 luck 数组失效。
- **timeline 选点漂移**(一版含60岁天克地冲、一版无凶点):`yunsui.ts` 增「建议节点」白名单(每步大运最重引动+当前大运重级流年,标权重),海报 5 节点必须全部取自白名单、重级若存在必须入选。
### Changed 调整(用户定)
- **顺逆措辞**:运势重量一律「顺风/平路/逆风」+程度词,弃「凶/大凶/凶年」绝对断语;模板图例同步(吉→顺风/平→平路/凶→逆风);长文同规。
- 回归 +4:出口齐备/忌神非空/建议节点非空/天克地冲重级。诊断用盘未写入任何文件。

## [2.2.0] · 2026-07-03 · 用神裁决进算法层(治 open 边界盘不可复现)
### Added 新增
- **算法层用神候选裁决**(`bazi-enrich/yongshen.ts`):扶抑(按旺衰score)/调候(穷通宝鉴定例)/格局(十一格顺逆用定例)三线确定性计算,输出共识用神、收敛标记、边界盘标记(|score|≤2或中和或置信度非高)与**出文协议**——LLM 只转述不取舍,与"排盘必须走算法层"铁律对齐。chart.txt 新增「用神建议」块。
- **open 边界协议**:边界盘/不收敛强制「体用两分」——护体线(调候/扶抑)与发用线(格局)并陈+⚖分歧标注+置信度照抄,禁止单选;同盘重复生成用神五行结论必须一致。
- **海报 schema 扩容**:`yongshen.yong_html` 支持『护体:X<br>发用:Y』双行;新增 `divergence_note` 字段(边界盘必填),模板新增 ys-divergence 位(空则隐藏);用/喜/忌/调候各字段限定取自算法层候选。
- 回归用例 +5:边界盘三线齐备/标记正确/两次计算逐字一致(可复现性)/身弱盘扶抑取印比(以合成干支组合构造,不含任何真实生辰)。
### 不改
- 排盘/旺衰/神煞算法本身(报告确认两次一致,无 bug);固定流派单镜片模式。

## [2.1.4] · 2026-07-03 · 可证伪事实护栏(用户反馈修)
### Fixed 修复
- **行为断言事故**:实际用户(Sonnet 5)收到「你的体质偏静、不太爱动」——本人恰好热爱运动。根因:模型把命理能量信号外推成可当场证伪的现实行为断言,弱模型缺显式护栏。新增全局规则(长文+海报双侧):禁止断言可证伪的行为/生活事实(运动习惯/胖瘦/熬夜/烟酒/婚育/职业/收入),命理只表述倾向与需求;附句式转换表供弱模型照抄——「断需求不断行为,断代价不断习惯,断倾向不断事实」。

## [2.1.4] · 2026-07-03 · 现实断言边界(用户反馈修复)
### Fixed 修复
- **断言对象越界**:实测反馈——健身爱好者被断言「体质偏静、不太爱动」。根因:模型从"禀赋倾向"外推到"可当场证伪的现实行为"。新增强制规则(长文+海报双侧,带正反例 few-shot):运动/饮食/作息/婚育/职业/病史等现实行为与事实**禁止断言**,一律改写为禀赋句/回报句/条件句;性格、相处、画像等盘面可断项**锐度不降**——判断锐度不变,判断对象收窄。

## [2.1.3] · 2026-07-03 · 正缘画像句式定型
### Changed 调整(用户定)
- 画像句式固定为「你适合的另一半更可能是一个{特质}、{特质}、{特质}的{男生/女生}」;年龄倾向必须择一明确判断(年长/年轻/同龄),禁止骑墙表述;**画像整句加粗标绿**;画像后必跟一句「他/她会怎么对待你」的相处描写。金标样例同步定型。

## [2.1.2] · 2026-07-03 · 正缘画像 + 变动类信号中性化
### Changed 调整(用户定)
- **正缘画像**:婚恋解读(长文+海报)必含一句 high-level 正缘画像——年龄倾向+性格气质+相处角色(如「你适合的另一半更可能是比你年长、有担当、性格柔和的类型」),依据配偶宫/夫妻星/日支藏干推出;金标婚恋样例同步补画像句。
- **变动≠凶**:"家庭/感情/居住易有明显变动"类表述默认中性不标红——变动可以是升迁置业良缘等好的大变动;仅明确指向破败损伤才红,指向机遇升级可绿;长文侧同步禁止把变动默认作凶断。

## [2.1.1] · 2026-07-03 · 决策门阻断化(修"开局不问流派/形态")
### Fixed 修复
- **决策门被跳过**:用户开场自带生辰时,Agent 常判定"类型已隐含"并默认代选流派与形态直接排盘。根因:①原约束是"避免 token 消耗"的建议语气,非阻断语气;②未规定"隐含类型≠三项都选"。现于 SKILL.md 顶部增**开工前置门禁**——三项(类型/流派/形态)未齐禁止 run-chart 与解读、缺项一条消息问齐、"都听你的"才可按默认并声明;关键约束第 2 条同步改为阻断表述。
- 清理 Step0 流派列表残留的"⑤新派·段氏盲派"(v2.0 并派漏改),现为五项。

## [2.1.0] · 2026-07-03 · 金标样例库 + 财路判定规则(反同质化)
### Added 新增
- **金标样例库扩充**:性格金标之外,新增事业/婚恋/健康/合冲刑害/运岁五段金标范文(同一张盘互相咬合),全部带完整红绿着色标记嵌入提示词 few-shot;口语度校准规则(亲切不轻佻、建议不加前缀、健康建议一律habit养成角度)。
- **财路判定规则**:实测 40 随机盘中真正具备「食伤生财」结构者仅 22%(财不透者达 57%),而解读几乎全写"以才华生财"——属措辞层坍缩。现强制按盘面十神结构选财路叙事:食伤生财→技艺变现 / 官杀配财→职权俸禄 / 印重→资格背书 / 比劫配财→渠道合伙 / 财通根身强→资产经营 / 财不透→以事功积累论;主叙事只取链路最完整一条。长文与海报双侧生效。

## [2.0.1] · 2026-07-03 · 二轮自测修复
### Fixed 修复
- **无效日期静默滚动**：2/30、非闰年 2/29、4/31 等此前被 JS Date 滚成合法日期并排出错盘("一步错满盘垮"的最坏形态)。`run-chart` 增输入校验:年(1900-2100)/月/日(按月天数含闰年)/时/分逐项校验,非法即报错退出;农历日≤30。
- **`--lineage=open` 丢派系侧重**：open 镜片视图重算时未带 `lineage_weights`,导致该模式下 chart.txt 无「重用/参用/不用」标注。现 open 镜片直接复用中立全集(含侧重字段)。
- 作用关系流派视图对 `duanshi` 别名统一归一化为盲派。
### QA 备注
- 二轮矩阵:极端年份(1901/2099/1899拒)、晚子时口径确认(23点换日派,已列路线图做成开关)、缺参数/13月/25时/61分全部正确拒绝、闰年2/29放行、无 analysis 渲染完整、v1.x 旧 chart.json 向后兼容(dump/render 均可用)。

## [2.0.0] · 2026-07-03 · 五派镜片 + 古法十干补全 + 判词规格
### Changed 重大调整
- **段氏并入盲派【用户定】**：流派镜片 6→5(子平/滴天髓/神峰/盲派/不限)。盲派镜片内含段建业取象体系(取象六法/断句风格/穿害尤重)，**凡段氏特有技法得出的分析点在解读中标注〔段氏〕**；`--lineage=duanshi` 保留为别名(自动按盲派计算)，旧命令与测试不受影响；神煞「派系侧重」由五列并为四列(盲派(含段氏))。
- **海报判词规格【用户定】**：`archetype_name` 改为 7 字判词或 4+4 字对仗，高度总结命主特性、华丽偏积极(如『金水雕龙藏锋客』『厚土载金·静水流深』)。
### Fixed 修复(文献补全)
- **文昌贵·古法甲乙句补全**：甲见巳、乙见亥——据宋·廖中《五行精纪》卷十三「甲人蛇口乙猪头,丙狗丁龙戊向猴,己午庚寅辛未贵,六壬卯位癸逢牛」(其丙-癸与本 skill 既有三命通会表逐字一致，可信度高)。古法十干齐，`unverified_keys` 清零。
- **福星贵人·古法丙–癸补全**：按三命通会 6.67 原文「余倒推」+《甲丙相邀入虎乡歌》(以年论,见五行精纪卷十三)配年干真食神：丙戊寅/戊子、丁己亥、戊庚申、己辛未、庚壬午、辛癸巳、壬甲辰、癸乙丑。`unverified_keys` 清零；路线图两项文献欠账全部清偿。

## [1.6.5] · 2026-07-03 · 吉凶着色金标化
### Changed 调整
- **着色规则重写【用户金标】**:着色单位=完整特质短语(可整个子句)、密度不设上限——正面特质全绿、弱点风险(含解释子句)全红;盘面证据与中性建议不着色。原「每字段1~3处只标关键词」作废。金标样例(戊土性格段,7绿2红)嵌入提示词作 few-shot。

## [1.6.4] · 2026-07-03 · 日主固定句式
### Changed 调整
- **海报日主区固定句式【用户定】**:『{日干}{五行}，属{意象}，特性是…，意味着你…，最强的能力是{绿标}，但{红标注意点}。』并内置十天干意象定表(甲=参天大树、丁=星星之火/烛火、戊=高山之土…)防意象漂移;能力短语固定绿标、注意点固定红标。

## [1.6.3] · 2026-07-03 · 自测发现两处修复
### Fixed 修复
- **海报神煞未按流派过滤**：`render --mode=bazi` 的四柱神煞行与神煞速览此前一律用中立全集——盲派海报冒出天医/血刃、子平海报冒出红鸾(MODERN 层)。现流派镜片优先(`神煞.lineage.hits`),与长文一致;不限流派仍用全集。
- **未起运盘流年条全裸横杠**:当前年不在任何大运内(如幼儿盘)时,流年十格全为「-」。现自动合成当前年起 10 个流年干支并加头注「尚未起运·列当前年起十年」。
### QA 备注
- 自测矩阵:立春前 00:30 早子时盘(年月柱正确退甲子/丁丑)、盲派/子平/不限三镜片、未起运盘、农历盘、综合海报回归、模板门禁、残留占位符为零;胎元/命宫对三盘手算复核一致。

## [1.6.2] · 2026-07-03 · Bug 修复(用户报告 BUG1-5) + 海报体验三项
### Fixed 修复
- **BUG-1(严重) 模板截断**：`report-bazi-poster.html` 尾部(宜业值/调候行/容器闭合/页脚/`</html>`)在一次挂载同步中被截失,已重建并校验(div 322/322,section 9/9);新增 `fixtures/check-template.ts` 完整性校验(结尾/开闭平衡/关键占位符),纳入打包流程强制门禁。
- **BUG-2 meta.name Schema 漂移**：`bazi-poster.md` 增 `meta.name`;`render.js` 增 `--name` CLI 兜底;无名默认「命主」不再裸 `-`。
- **BUG-3 胎元/命宫缺算**：`run-chart` 补算——胎元=月干进一/月支进三;命宫=14/26 减(月数+时数)得支、五虎遁取干;render 注入头部。
- **BUG-4 dayun_luck 规格不一致**：Schema 改「与盘内大运步数一致(通常9~10)」;render 对 dayun_luck/liunian_luck 长度不符输出 warning(不中断)。
- **BUG-5 CLI 帮助缺参**：Usage 补 `--mode`/`--currentYear`/`--name`。
### Changed 调整(用户定)
- **海报运岁引动瘦身**:流年只呈现从 currentYear 起未来 5 年(数据层仍全量,长文不受影响)。
- **标绿范围扩大**:绿=吉利之事+本人好特质(天赋/优点/性格亮点);红=需注意/风险。
- **开运调理扩充**:新增宜往(地方)/宜置(物件)/宜学(技能)三行+2~3句综合开运建议段(`kaiyun.note_html`),不再空旷。

## [1.6.1] · 2026-07-03 · 海报文字分层精简 + 吉凶着色 + 五行色块
### Changed 调整
- **字段三分层【用户定】**：日主/格局/五行/十神/用神依据改〔两句〕制——一句盘面是什么+一句「所以你…」；性格/事业/婚恋/健康保持 6~10 句详写；合冲刑害/运岁/神煞精读段 3~5 句。
- **吉凶着色**：解读关键词吉利包 `hl-good`(加粗绿)、需注意包 `hl`(加粗红)，每字段 1~3 处。
- **五行色块**：用/忌/喜/调候元素由渲染层自动包 `wx-chip`(连续同五行合并一块)，LLM 输出纯文字即可；模板新增五行底色样式。

## [1.6.0] · 2026-07-03 · 海报长图化+对话口吻 / 解读重意义 / 神煞三段式 / 穷通宝鉴纳入
### Added 新增
- **八字海报新增两区块**：05「作用关系·合冲刑害」（裁决行算法注入 + LLM 解读段）、06「运岁引动」（大运/流年引动行 + 解读段）；08 神煞速览增整体解读段；`render.ts --mode=bazi` 注入 `hechong.*`/`yunsui.*`/`shensha.reading_html`；模板增 hc-row 样式与状态配色。
- **神煞三段式解读（长文·用户定）**：①白话等级(一线核心/常用/复合贵格/凶煞警示/现代偏门)②主什么(略)③**这意味着你什么(详写加粗)**；via/tier/needs_review 等内部字段不再向用户展示，待核项转保守措辞；**open 模式必须标注派系强弱分歧**。
- **派系侧重数据**：open 全集每个神煞命中附 `lineage_weights`(五传统派权重)，chart.txt 打印「重用/参用/不用」——供分歧标注落地。
- **《穷通宝鉴》正式纳入**：调候层一律以其为基准文献点名引用（算法层调候字段即穷通宝鉴 120 格）；六派 literature 补「穷通宝鉴(调候参照)」。
### Changed 调整
- **海报**：〔段落〕下限提到 6~10 句(约200字起)、长图不限一页；**全部解读改第二人称对话口吻**（bazi/zonghe 两模板提示词同步）。
- **长文**：新增全局强制规则「实际意义优先」——盘面结构表述只作一句话证据引注，每处必须跟至少两倍篇幅的现实意义解读；全程第二人称口吻。

## [1.5.0] · 2026-07-03 · 合冲刑害作用维度成为主功能
### Added 新增
- **检测精化**（`zhi-relations`/`gan-relations`）：半合/拱合修正（生旺·旺库=半合含旺神，生库=虚拱旺神——原实现把邻支二合误标拱合）；新增相破（六破，巳申/寅亥标注与合刑重叠力轻）；天干争合(妒合)标注。
- **作用裁决引擎**（`bazi-enrich/interactions.ts`）：对检出关系按流派规则集判【生效/减力/被解/被绊/合而化/合而不化(绊)/成局/半局/虚拱】＋依据。规则项：贪合忘冲/合解冲、冲破合(当令)、会局解刑冲、库冲是否作开库、穿(害)权重、合化判定(化神当令+有引,从严)。【用户定:按流派切换规则集,仅 open 标注分歧】——`lineages.json` 六派各配 `interaction_policy`（子平=格局喜忌口径,滴天髓=库宜开/合多为绊,神峰=病药,盲派/段氏=做功·穿重·不概解,open=通则+`divergence` 逐条并陈）。中立视图固定 open 通则,排盘不随派变。
- **运岁引动**（`bazi-enrich/yunsui.ts`）：十步大运×原局＋当前大运十流年×(原局+大运)——支冲(冲提纲/库冲标注)/合/刑/穿/破、凑成三合、凑全三刑、干合干克、岁运并临、天克地冲、伏吟；`run-chart` 新增 `--currentYear`（缺省=系统年）。
- **文本盘**：`chart.txt` 新增「作用关系(裁决)」（open 视图＋流派视图对照）与「运岁引动」块；`bazi-prompt.md` 增强制规则——解读必须照抄裁决状态、分歧并陈、应期不得凭空造。
- 回归测试：`fixtures/test-relations.ts` 17 项（半合/拱/破/争合/合化、贪合忘冲派系差、库冲派系差、穿权重、冲提纲/岁运并临/天克地冲/伏吟），与神煞 13 例全部通过。
### Changed 调整
- 各派 `prompt_inject`（该字段 v3.11.1 起更名为 `pai_panci`，此处保留当时名以存史） 追加合冲刑害读法（子平合去喜忌/滴天髓战绊库/神峰病药/盲派段氏做功穿）；`core_techniques` 补关键词。

## [1.4.3] · 2026-07-03 · 只读安装目录零依赖运行（用户报告修复）
### Fixed 修复
- **装好即用**：用户本地安装后,Agent 因技能目录只读、无 node_modules 而被迫复制目录+现场 npm install。现新增 `calculator/dist-bundle/`——`run-chart.js`/`dump-text.js`/`render.js` 三个 **esbuild 自足单文件**（lunar-typescript 等依赖已打入),`node` 直跑、零安装、兼容只读目录（数据文件自上层目录解析,已在只读环境实测）。
- **锁文件 403**：`package-lock.json` 原钉在 registry.npmmirror.com（部分网络 403 拒装）,已重生成为 registry.npmjs.org。
### Changed 调整
- `SKILL.md`：Step1/2/3 运行命令全部改为 dist-bundle 零安装路径,增「只读目录须知」（不 cd 进技能目录写文件、不就地 npm install、`--output` 指向会话工作目录）；依赖检查段改为「bundle 优先,重编译才复制到可写目录安装」。

## [1.4.2] · 2026-07-03 · 天乙/太极/国印年干兼查落地（文实一致修复）
### Fixed 修复
- **base 与引擎不符**：天乙/太极/国印三项 `base` 声明「日干(或年干)」，但 `dayGan` 引擎只查日干（用户报告：年干乙命之申未被报出）。现按 `_meta` 并查政策精神落地 `also_year_gan`：**日干为主、年干为兼，union 命中，via 标注「日干X/年干Y」来源**（年干=日干时不重复计）；天乙昼夜贵当值标注仍只挂日干种子。
### Changed 调整
- 回归 fixtures 12→13 例：新增边界用例「年干中、日干不中」（乙年甲日，申@时经年干命中）；c1/c4/v3_A 三处旧断言按新政策修正（天乙年干甲→未、己→子；太极年干庚→寅）。全部通过。

## [1.4.1] · 2026-07-03 · README 神煞清单更新 + 海报解读段落制
### Changed 调整
- **README 神煞清单 24→43**：按 tier 重排（T1 核心 9 / T2 常用 15 / 复合 2 / T3 凶煞 5 / MODERN 现代 12），注明 MODERN 仅『不限流派』启用与多版本「任一命中即写」政策；版本徽章 v1.4.0。
- **海报解读段落制【用户定】**：`bazi-poster.md`（v1.1）解读类字段（日主/格局/五行/十神机理与白话/用神推导/性格/事业/婚恋/健康）由字数上限改为〔段落〕制——**不限篇幅，但至少一个成型段落（3~5 句起），禁止一句话敷衍**；大运/流年总评放宽为 1~3 句。标签类字段（标题/用忌神名/开运项/时间轴条目）仍守上限保版式。`zonghe-poster.md` 同步：`stage_yinzheng.conclusion` 改段落制，对照矩阵等表格单元格保持紧凑。`SKILL.md` Step3 增全局长度规则说明。模板经核验用 min-height、无截断逻辑，长段自动撑开。

## [1.4.0] · 2026-07-03 · 神煞大扩充（三命通会补缺 + 现代 MODERN 层）
### Added 新增
- **三命通会侧补缺 5 项**：学堂词馆（纳音长生/临官，正偏判定 + 干禄词馆变体表）、天福贵人（官星坐禄）、日贵（4 日）、日德（5 日）、福德秀气（15 日）。
- **二德之合**：天德合、月德合（由天德/月德表推五合/六合）。
- **天乙贵人昼夜分层**：阳贵/阴贵分表（协纪辨方系），卯~申时昼生用阳贵当值、酉~寅时夜生用阴贵当值；命中标注当值与否，不改判定。
- **月德「日上见」标注**：《三命通会》要日上见——via 标注「日干见(足格)/非日干见(力减)」。
- **MODERN 现代神煞层 12 项（仅『不限流派 open』启用，引擎强制白名单门禁）**：红鸾、天喜、童子煞（季节法+纳音法 union）、孤鸾煞（核心五日+扩展三日）、阴差阳错、十恶大败、四废、天罗地网（古法纳音+互见法 union）、天医、流霞（两版 union）、血刃（月支法，exclude_base 防自命中）、天厨贵人（法诀版+子平版 union）。多版本查法【用户定：任一命中即写】，via 标注所用口诀/版本。
- **新查法引擎 7 种**：`nayin_xuetang` / `yearZhi_to` / `tongzi` / `season_day` / `tianluo_diwang` / `yueZhi_prev` / `dayGan_multi`；`fixed_pillars` 增 `table_core`/`hour_note`，`yueZhi_to` 增 `exclude_base`。
- 神煞回归测试 7→12 例（新增 v3 覆盖 A/B 盘 + MODERN 门禁负例），全部通过。
### Changed 调整
- `shensha.json` 24→43 项；`_meta` 增 tier=MODERN 与多版本政策说明。
- `lineages.json`：open/ziping 白名单扩充；MODERN 仅 open 启用（传统流派自动隐藏）。
- `bazi-prompt.md` / `SKILL.md`：神煞支柱增 via 细化标注转述要求、MODERN 层呈现规则。

## [1.3.0] · 2026-06-30 · 段氏流派落地 + 八字独立海报
### Added 新增
- **八字独立海报**：新增单系统海报模板 `templates/report-bazi-poster.html` + 提示词 `prompts/bazi-poster.md` + `render.ts --mode=bazi` 分支；Step0「八字独立」像综合印证一样可选 **长文 / 海报 / 两者**。单系统，无双盘 verdict/consistency/conflict；盘面数据由算法层注入、LLM 只产解读字段。
- **段氏盲派流派落地**：据段建业《象的应用》《八字断句集》填入段氏（`duanshi_TODO`→`duanshi`）——取象六法（共象/化象/墓象/换象/带象/局象）+ 做功 + 断语；神煞并入「神煞象」、权重比照同源盲派并**注明《理象学》神煞类象专表未在所供资料、不臆造**。段氏镜片现可正常计算命中。
### Changed 调整
- `duanshi_TODO` 引用全部更新为 `duanshi`（lineages / SKILL / run-chart / bazi-prompt / README）。

## [1.2.0] · 2026-06-30 · 流派忠实度 & 文档重排
### Changed 调整
- **流派忠实度（解读层强化）**：选定流派后，模型**严格在该派 `core_techniques` 内、以该派文献（`literature`）为核心**分析，回避该派 `deemphasize` 判法，**不串派**；某结论该派文献无据则标存疑而非借他派——仅「不限 / open」才多派并陈。`bazi-prompt.md` 新增「流派忠实度」核心原则节，注入 `{{lineage.literature}}` / `{{lineage.deemphasize}}`；SKILL.md 同步约束。
- **文档**：README 框架重排——各模型安装说明收纳到「安装 / 使用指南」H2 下的子标题（H3），并新增顶部目录(TOC)。

## [1.1.0] · 2026-06-30 · 起法定版 & 古法交叉校验
### Added 新增
- **古法交叉校验（文昌 / 福星）**：通行版为命中主表；命中时再算一遍《三命通会》古法——古法亦合→标「古法亦合@柱」，古法无→标「古法无」，文献残损/未列全→标「未校验」（不编造）。
- **三奇严格顺布连珠**：由「集合相等」改为严格次第校验（倒乱不作奇，依《三命通会》卷三）；新增回归用例 c8（乱序不算）/ c9（顺序@月日时）。
### Changed 调整
- **国印贵人**：纳入，`needs_review → false`（通行国印表；《三命通会》《渊海》无载，属《协纪辨方》系）。
- **元辰**：核心起法（阳男阴女冲前一位 / 阴男阳女冲后一位）实现并验证，`needs_review → false`；「午未半之」经核读为示例描述、非独立算法。
### Fixed / 核验
- **红艳煞** 对齐《三命通会》卷三原文（戊子 / 壬巳 版）。
- **魁罡·戊辰 / 羊刃·阴干刃** 依《三命通会》默认关闭（卷六日魁罡四日不含戊辰；3.13「五阳干有刃、五阴干无刃」）。
- **三合 seed** 锁定「年 + 日并查」为正式政策。
- `needs_review: true` 归零；`schema-check` + 9 例 `test-shensha` 全绿。

## [1.0.0] · 2026-06-30 · 首个 Decoder 版（五层超级魔改）
> 基于 dzcmemory-web/bazi-ziwei-skill 重构发布，在其精准排盘内核之上叠加五层。
### Added 新增
- **神煞引擎**：24 神煞数据驱动起例（`shensha.json` + `shensha.ts`），写入算法层，带文献出处与 `needs_review` 标记。
- **流派镜片（6 派）**：子平 / 滴天髓 / 神峰通考 / 盲派 / 段氏(stub) / 不限；`--lineage` 仅换解读视角，**绝不改排盘**。
- **成长心态声明置顶**：每次八字分析开头必出（反「越算越不好」）。
- **输出模式 B**：总领速览 + 按需下钻（固定章号菜单、锚点防漂移、不诱导继续）。
- **文献核验底线**：起法分歧 / 查不到 → 标「⚠起法待核」，绝不编造。
- **回归测试**：`schema-check` 配置自检 + `fixtures` 神煞回归。
### 继承自基板
- 精准排盘（四柱 / 十神 / 大运 / 紫微十二宫 / 生年四化 / 大限）、enrich 补层、八字 / 紫微 / 综合印证、长文 + 海报输出。
