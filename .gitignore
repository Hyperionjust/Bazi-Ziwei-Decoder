# dependencies
node_modules/
calculator/node_modules/

# 编译产物:运行时一律走自足的 calculator/dist-bundle/,tsc 的 dist/ 只是本地开发中间物。
# v3.9.1 前它曾入库并随包分发,且停留在旧版 → 走 `node dist/run-chart.js` 会静默跑出旧逻辑。
calculator/dist/
dist-release/

# build / runtime artifacts
*.log
.DS_Store
Thumbs.db

# 排盘运行产物（用户生成的命盘 / 分析 / 报告，含个人生辰，勿入库）
*.tmp
chart.json
chart.txt
analysis.json
smoke.json
report.html
*-report.html
*-zonghe.html

# 校准命例与对照表（含真实生辰，勿入库；脚手架见 calculator/fixtures/calibration/）
*.case.json
calibration-report.md

# 但仓库自带的合成示例必须入库（白名单放行·整目录放行，避免 *-report.html 等规则误伤新增样例）
!examples/
!examples/**

# editor
.vscode/
.idea/
*.swp
